"use strict";(()=>{var j=(e,n,s)=>()=>{if(s)throw s[0];try{return e&&(n=e(e=0)),n}catch(a){throw s=[a],a}};var te=(e,n)=>()=>{try{return n||e((n={exports:{}}).exports,n),n.exports}catch(s){throw n=0,s}};var E,C,Oe,W=j(()=>{E=["owl2rl-eyeling.n3","shacl-core-eyeling.n3","shacl12-core-eyeling.n3","skos-entailment.n3"],C=[{file:"owl2rl-eyeling.n3",n3:`# Source: rules/owl2rl-eyeling.n3
# OWL 2 RL/RDF rules in Notation3 (N3) for Eyeling
# -----------------------------------------------------------------------------
# This file is an implementation-oriented OWL 2 RL/RDF ruleset targeting
# current Eyeling, including the dt: datatype builtins added after issue #18.
#
# Scope:
#   * Implements the OWL 2 RL rule core that is expressible in Eyeling N3.
#   * Uses log:skolem to create stable helper nodes for internal n-ary facts.
#   * Implements property-chain reasoning in N3 using recursive chain helpers.
#   * Includes OWL 2 RL datatype rules using Eyeling's dt: value-space builtins.
#
# Important caveats:
#   * This is an OWL 2 RL materialization ruleset, not an OWL 2 DL tableau reasoner.
#   * Some OWL 2 RL features require RDF list/property-chain helper rules and can be
#     expensive on large graphs.
#   * Inconsistency rules derive owlrl:Inconsistency resources instead of false.

@prefix rdf:   <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:  <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl:   <http://www.w3.org/2002/07/owl#> .
@prefix xsd:   <http://www.w3.org/2001/XMLSchema#> .
@prefix log:   <http://www.w3.org/2000/10/swap/log#> .
@prefix math:  <http://www.w3.org/2000/10/swap/math#> .
@prefix dt:    <https://eyereasoner.github.io/eyeling/datatype#> .
@prefix list:  <http://www.w3.org/2000/10/swap/list#> .
@prefix owlrl: <https://example.org/owlrl-n3#> .

#################################################################
# 0. Vocabulary used by this ruleset
#################################################################

owlrl:Inconsistency a rdfs:Class .
owlrl:listRoot a rdf:Property .
owlrl:intersectionListRoot a rdf:Property .
owlrl:longIntersectionListRoot a rdf:Property .
owlrl:keyListRoot a rdf:Property .
owlrl:propertyChainRoot a rdf:Property .
owlrl:listMember a rdf:Property .
owlrl:listPair a rdf:Property .
owlrl:left a rdf:Property .
owlrl:right a rdf:Property .
owlrl:allListClassTypes a rdf:Property .
owlrl:sameValuesForProperties a rdf:Property .
owlrl:propertyChainHolds a rdf:Property .
owlrl:pathChain a rdf:Property .
owlrl:pathSubject a rdf:Property .
owlrl:pathObject a rdf:Property .
owlrl:term a rdf:Property .
owlrl:canonicalLiteral a rdf:Property .
owlrl:rule a rdf:Property .
owlrl:term1 a rdf:Property .
owlrl:term2 a rdf:Property .
owlrl:term3 a rdf:Property .
owlrl:term4 a rdf:Property .
owlrl:term5 a rdf:Property .

# Axiomatic OWL facts used by conformance tests. Application output filters remove
# reflexive equality and internal helper facts by default.
owl:sameAs rdf:type owl:ReflexiveProperty .
owl:differentFrom rdf:type owl:SymmetricProperty .
owl:NamedIndividual rdf:type owl:Class ;
  rdfs:subClassOf owl:Thing .

#################################################################
# 1. OWL 2 RL supported datatype declarations (dt-type1)
#################################################################

rdf:PlainLiteral       a rdfs:Datatype .
rdf:XMLLiteral         a rdfs:Datatype .
rdfs:Literal           a rdfs:Datatype .
xsd:decimal            a rdfs:Datatype .
xsd:integer            a rdfs:Datatype .
xsd:nonNegativeInteger a rdfs:Datatype .
xsd:nonPositiveInteger a rdfs:Datatype .
xsd:positiveInteger    a rdfs:Datatype .
xsd:negativeInteger    a rdfs:Datatype .
xsd:long               a rdfs:Datatype .
xsd:int                a rdfs:Datatype .
xsd:short              a rdfs:Datatype .
xsd:byte               a rdfs:Datatype .
xsd:unsignedLong       a rdfs:Datatype .
xsd:unsignedInt        a rdfs:Datatype .
xsd:unsignedShort      a rdfs:Datatype .
xsd:unsignedByte       a rdfs:Datatype .
xsd:float              a rdfs:Datatype .
xsd:double             a rdfs:Datatype .
xsd:string             a rdfs:Datatype .
xsd:normalizedString   a rdfs:Datatype .
xsd:token              a rdfs:Datatype .
xsd:language           a rdfs:Datatype .
xsd:Name               a rdfs:Datatype .
xsd:NCName             a rdfs:Datatype .
xsd:NMTOKEN            a rdfs:Datatype .
xsd:boolean            a rdfs:Datatype .
xsd:hexBinary          a rdfs:Datatype .
xsd:base64Binary       a rdfs:Datatype .
xsd:anyURI             a rdfs:Datatype .
xsd:dateTime           a rdfs:Datatype .
xsd:dateTimeStamp      a rdfs:Datatype .

xsd:integer rdfs:subClassOf xsd:decimal .
xsd:long rdfs:subClassOf xsd:integer .
xsd:int rdfs:subClassOf xsd:long .
xsd:short rdfs:subClassOf xsd:int .
xsd:byte rdfs:subClassOf xsd:short .
xsd:nonPositiveInteger rdfs:subClassOf xsd:integer .
xsd:negativeInteger rdfs:subClassOf xsd:nonPositiveInteger .
xsd:nonNegativeInteger rdfs:subClassOf xsd:integer .
xsd:positiveInteger rdfs:subClassOf xsd:nonNegativeInteger .
xsd:unsignedLong rdfs:subClassOf xsd:nonNegativeInteger .
xsd:unsignedInt rdfs:subClassOf xsd:unsignedLong .
xsd:unsignedShort rdfs:subClassOf xsd:unsignedInt .
xsd:unsignedByte rdfs:subClassOf xsd:unsignedShort .
xsd:dateTimeStamp rdfs:subClassOf xsd:dateTime .
xsd:normalizedString rdfs:subClassOf xsd:string .
xsd:token rdfs:subClassOf xsd:normalizedString .
xsd:language rdfs:subClassOf xsd:token .
xsd:Name rdfs:subClassOf xsd:token .
xsd:NCName rdfs:subClassOf xsd:Name .
xsd:NMTOKEN rdfs:subClassOf xsd:token .

#################################################################
# 2. Built-in annotation properties (prp-ap)
#################################################################

rdfs:label                  a owl:AnnotationProperty .
rdfs:comment                a owl:AnnotationProperty .
rdfs:seeAlso                a owl:AnnotationProperty .
rdfs:isDefinedBy            a owl:AnnotationProperty .
owl:deprecated              a owl:AnnotationProperty .
owl:versionInfo             a owl:AnnotationProperty .
owl:priorVersion            a owl:AnnotationProperty .
owl:backwardCompatibleWith  a owl:AnnotationProperty .
owl:incompatibleWith        a owl:AnnotationProperty .

#################################################################
# 3. RDF list and path helpers
#################################################################

# Mark OWL/RDF list roots that this ruleset needs to inspect.  This keeps
# builtins such as rdf:first/rdf:rest called with a bound list term in Eyeling.
{ ?x owl:intersectionOf ?list . } => { ?list owlrl:listRoot true ; owlrl:intersectionListRoot true . } .

# Short intersections are handled directly.  Mark only intersections of length
# >= 5 for the recursive allListClassTypes helper and propagate that mark to
# their tails so the helper can build suffix states.
{ ?x owl:intersectionOf ?list .
  ?list rdf:rest ?tail1 .
  ?tail1 rdf:rest ?tail2 .
  ?tail2 rdf:rest ?tail3 .
  ?tail3 rdf:rest ?tail4 .
  ?tail4 rdf:first ?class5 . }
=> { ?list owlrl:longIntersectionListRoot true . } .

{ ?x owl:unionOf ?list . }        => { ?list owlrl:listRoot true . } .
{ ?x owl:oneOf ?list . }          => { ?list owlrl:listRoot true . } .
{ ?x owl:members ?list . }        => { ?list owlrl:listRoot true . } .
{ ?x owl:distinctMembers ?list . }=> { ?list owlrl:listRoot true . } .
{ ?x owl:hasKey ?list . }         => { ?list owlrl:listRoot true ; owlrl:keyListRoot true . } .
{ ?x owl:propertyChainAxiom ?list . } => { ?list owlrl:listRoot true . } .

# The common short property-chain cases are handled directly below.  Mark only
# chains of length >= 4 for the generic recursive helper, otherwise list-heavy
# data with many rdf:rest triples pays the helper cost for no benefit.
{ ?x owl:propertyChainAxiom ?list .
  ?list rdf:rest ?tail1 .
  ?tail1 rdf:rest ?tail2 .
  ?tail2 rdf:rest ?tail3 .
  ?tail3 rdf:first ?p4 . }
=> { ?list owlrl:propertyChainRoot true . } .

# Propagate list-root status to non-empty tails.
{ ?list owlrl:listRoot true .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?head . }
=> { ?tail owlrl:listRoot true . } .

{ ?list owlrl:intersectionListRoot true .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?head . }
=> { ?tail owlrl:intersectionListRoot true . } .

{ ?list owlrl:longIntersectionListRoot true .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?head . }
=> { ?tail owlrl:longIntersectionListRoot true . } .

{ ?list owlrl:keyListRoot true .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?head . }
=> { ?tail owlrl:keyListRoot true . } .

{ ?list owlrl:propertyChainRoot true .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?head . }
=> { ?tail owlrl:propertyChainRoot true . } .

# list membership: LIST[h, ..., x, ...]
{ ?list owlrl:listRoot true .
  ?list rdf:first ?member . }
=> { ?list owlrl:listMember ?member . } .

{ ?list owlrl:listRoot true .
  ?list rdf:rest ?tail .
  ?tail owlrl:listMember ?member . }
=> { ?list owlrl:listMember ?member . } .

# ordered pairs i<j in a list. The pair node is stable via log:skolem.
{ ?list owlrl:listRoot true .
  ?list rdf:first ?left .
  ?list rdf:rest ?tail .
  ?tail owlrl:listMember ?right .
  (owlrl:listPair ?list ?left ?right) log:skolem ?pair . }
=> { ?list owlrl:listPair ?pair .
     ?pair owlrl:left ?left ;
           owlrl:right ?right . } .

{ ?list owlrl:listRoot true .
  ?list rdf:rest ?tail .
  ?tail owlrl:listPair ?pair . }
=> { ?list owlrl:listPair ?pair . } .

# allListClassTypes: tuple (?individual ?list) is true iff the individual
# is rdf:type every class in the non-empty RDF list.
{ ?list owlrl:longIntersectionListRoot true .
  ?list rdf:first ?class .
  ?list rdf:rest rdf:nil .
  ?individual rdf:type ?class .
  (owlrl:allListClassTypes ?individual ?list) log:skolem ?state . }
=> { ?state owlrl:allListClassTypes true ;
           owlrl:pathSubject ?individual ;
           owlrl:pathObject ?list . } .

{ ?list owlrl:longIntersectionListRoot true .
  ?list rdf:first ?class .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?tailFirst .
  ?individual rdf:type ?class .
  (owlrl:allListClassTypes ?individual ?tail) log:skolem ?tailState .
  ?tailState owlrl:allListClassTypes true .
  (owlrl:allListClassTypes ?individual ?list) log:skolem ?state . }
=> { ?state owlrl:allListClassTypes true ;
           owlrl:pathSubject ?individual ;
           owlrl:pathObject ?list . } .

# sameValuesForProperties: tuple (?x ?y ?propertyList) is true iff ?x and ?y
# share at least one identical value for every property in the non-empty list.
# Equality replacement rules make sameAs-equal values count as identical after closure.
{ ?plist owlrl:keyListRoot true .
  ?plist rdf:first ?p .
  ?plist rdf:rest rdf:nil .
  ?x ?p ?z .
  ?y ?p ?z .
  (owlrl:sameValuesForProperties ?x ?y ?plist) log:skolem ?state . }
=> { ?state owlrl:sameValuesForProperties true . } .

{ ?plist owlrl:keyListRoot true .
  ?plist rdf:first ?p .
  ?plist rdf:rest ?tail .
  ?tail rdf:first ?tailFirst .
  ?x ?p ?z .
  ?y ?p ?z .
  (owlrl:sameValuesForProperties ?x ?y ?tail) log:skolem ?tailState .
  ?tailState owlrl:sameValuesForProperties true .
  (owlrl:sameValuesForProperties ?x ?y ?plist) log:skolem ?state . }
=> { ?state owlrl:sameValuesForProperties true . } .

# property-chain helper. A path node states that the RDF list ?chain relates
# ?x to ?y by following the properties in the list in order.
{ ?chain owlrl:propertyChainRoot true .
  ?chain rdf:first ?p .
  ?chain rdf:rest rdf:nil .
  ?x ?p ?y .
  (owlrl:propertyChainHolds ?chain ?x ?y) log:skolem ?path . }
=> { ?path owlrl:propertyChainHolds true ;
           owlrl:pathChain ?chain ;
           owlrl:pathSubject ?x ;
           owlrl:pathObject ?y . } .

{ ?chain owlrl:propertyChainRoot true .
  ?chain rdf:first ?p .
  ?chain rdf:rest ?tail .
  ?tail rdf:first ?tailFirst .
  ?x ?p ?z .
  ?tailPath owlrl:propertyChainHolds true ;
            owlrl:pathChain ?tail ;
            owlrl:pathSubject ?z ;
            owlrl:pathObject ?y .
  (owlrl:propertyChainHolds ?chain ?x ?y) log:skolem ?path . }
=> { ?path owlrl:propertyChainHolds true ;
           owlrl:pathChain ?chain ;
           owlrl:pathSubject ?x ;
           owlrl:pathObject ?y . } .

#################################################################
# 4. Equality rules (eq-*)
#################################################################

# eq-ref is intentionally not materialized as a rule. Reflexive owl:sameAs
# triples are filtered from application output and can be added by conformance
# harnesses when required; deriving them for every term makes list- and
# literal-heavy data much more expensive without affecting non-reflexive
# equality substitution.

# eq-sym
{ ?x owl:sameAs ?y . }
=> { ?y owl:sameAs ?x . } .

# eq-trans
{ ?x owl:sameAs ?y .
  ?y owl:sameAs ?z . }
=> { ?x owl:sameAs ?z . } .

# eq-rep-s
{ ?s owl:sameAs ?s2 .
  ?s ?p ?o . }
=> { ?s2 ?p ?o . } .

# eq-rep-p
{ ?p owl:sameAs ?p2 .
  ?s ?p ?o . }
=> { ?s ?p2 ?o . } .

# eq-rep-o
{ ?o owl:sameAs ?o2 .
  ?s ?p ?o . }
=> { ?s ?p ?o2 . } .

# eq-diff1
{ ?x owl:sameAs ?y .
  ?x owl:differentFrom ?y .
  (owlrl:eq-diff1 ?x ?y) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:eq-diff1 ;
          owlrl:term1 ?x ;
          owlrl:term2 ?y . } .

# eq-diff2: AllDifferent/owl:members
{ ?ad a owl:AllDifferent .
  ?ad owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?zi ;
        owlrl:right ?zj .
  ?zi owl:sameAs ?zj .
  (owlrl:eq-diff2 ?ad ?zi ?zj) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:eq-diff2 ;
          owlrl:term1 ?ad ;
          owlrl:term2 ?zi ;
          owlrl:term3 ?zj . } .

# eq-diff3: AllDifferent/owl:distinctMembers
{ ?ad a owl:AllDifferent .
  ?ad owl:distinctMembers ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?zi ;
        owlrl:right ?zj .
  ?zi owl:sameAs ?zj .
  (owlrl:eq-diff3 ?ad ?zi ?zj) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:eq-diff3 ;
          owlrl:term1 ?ad ;
          owlrl:term2 ?zi ;
          owlrl:term3 ?zj . } .

#################################################################
# 5. Property axiom rules (prp-*)
#################################################################

# prp-dom
{ ?p rdfs:domain ?c .
  ?x ?p ?y . }
=> { ?x rdf:type ?c . } .

# prp-rng
{ ?p rdfs:range ?c .
  ?x ?p ?y . }
=> { ?y rdf:type ?c . } .

# prp-fp
{ ?p rdf:type owl:FunctionalProperty .
  ?x ?p ?y1 .
  ?x ?p ?y2 . }
=> { ?y1 owl:sameAs ?y2 . } .

# prp-ifp
{ ?p rdf:type owl:InverseFunctionalProperty .
  ?x1 ?p ?y .
  ?x2 ?p ?y . }
=> { ?x1 owl:sameAs ?x2 . } .

# prp-irp
{ ?p rdf:type owl:IrreflexiveProperty .
  ?x ?p ?x .
  (owlrl:prp-irp ?p ?x) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:prp-irp ;
          owlrl:term1 ?p ;
          owlrl:term2 ?x . } .

# prp-symp
{ ?p rdf:type owl:SymmetricProperty .
  ?x ?p ?y . }
=> { ?y ?p ?x . } .

# prp-asyp
{ ?p rdf:type owl:AsymmetricProperty .
  ?x ?p ?y .
  ?y ?p ?x .
  (owlrl:prp-asyp ?p ?x ?y) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:prp-asyp ;
          owlrl:term1 ?p ;
          owlrl:term2 ?x ;
          owlrl:term3 ?y . } .

# prp-trp
{ ?p rdf:type owl:TransitiveProperty .
  ?x ?p ?y .
  ?y ?p ?z . }
=> { ?x ?p ?z . } .

# prp-rp
{ ?p rdf:type owl:ReflexiveProperty .
  ?x rdf:type owl:NamedIndividual . }
=> { ?x ?p ?x . } .

# prp-spo1
{ ?p1 rdfs:subPropertyOf ?p2 .
  ?x ?p1 ?y . }
=> { ?x ?p2 ?y . } .

# prp-spo2: property chains.  Most practical OWL 2 RL chains are short, so
# length 1-3 chains are materialized directly without helper path nodes.
{ ?p owl:propertyChainAxiom ?chain .
  ?chain rdf:first ?p1 .
  ?chain rdf:rest rdf:nil .
  ?x ?p1 ?y . }
=> { ?x ?p ?y . } .

{ ?p owl:propertyChainAxiom ?chain .
  ?chain rdf:first ?p1 .
  ?chain rdf:rest ?tail1 .
  ?tail1 rdf:first ?p2 .
  ?tail1 rdf:rest rdf:nil .
  ?x ?p1 ?z .
  ?z ?p2 ?y . }
=> { ?x ?p ?y . } .

{ ?p owl:propertyChainAxiom ?chain .
  ?chain rdf:first ?p1 .
  ?chain rdf:rest ?tail1 .
  ?tail1 rdf:first ?p2 .
  ?tail1 rdf:rest ?tail2 .
  ?tail2 rdf:first ?p3 .
  ?tail2 rdf:rest rdf:nil .
  ?x ?p1 ?z1 .
  ?z1 ?p2 ?z2 .
  ?z2 ?p3 ?y . }
=> { ?x ?p ?y . } .

# Longer property chains use the recursive owlrl:propertyChainHolds helper above.
{ ?p owl:propertyChainAxiom ?chain .
  ?path owlrl:propertyChainHolds true ;
        owlrl:pathChain ?chain ;
        owlrl:pathSubject ?x ;
        owlrl:pathObject ?y . }
=> { ?x ?p ?y . } .

# A property-chain axiom P o P -> P is equivalent to transitivity for P.
{ ?p owl:propertyChainAxiom ?chain .
  ?chain rdf:first ?p .
  ?chain rdf:rest ?tail .
  ?tail rdf:first ?p .
  ?tail rdf:rest rdf:nil . }
=> { ?p rdf:type owl:TransitiveProperty . } .

# prp-eqp1/prp-eqp2
{ ?p1 owl:equivalentProperty ?p2 .
  ?x ?p1 ?y . }
=> { ?x ?p2 ?y . } .

{ ?p1 owl:equivalentProperty ?p2 .
  ?x ?p2 ?y . }
=> { ?x ?p1 ?y . } .

# prp-pdw
{ ?p1 owl:propertyDisjointWith ?p2 .
  ?x ?p1 ?y .
  ?x ?p2 ?y .
  (owlrl:prp-pdw ?p1 ?p2 ?x ?y) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:prp-pdw ;
          owlrl:term1 ?p1 ;
          owlrl:term2 ?p2 ;
          owlrl:term3 ?x ;
          owlrl:term4 ?y . } .

# If disjoint properties relate two subjects to the same value, the subjects are
# different. This RDF-Based consequence is exercised by the official OWL tests.
{ ?p1 owl:propertyDisjointWith ?p2 .
  ?x1 ?p1 ?y .
  ?x2 ?p2 ?y . }
=> { ?x1 owl:differentFrom ?x2 . } .

{ ?p1 owl:propertyDisjointWith ?p2 .
  ?x1 ?p2 ?y .
  ?x2 ?p1 ?y . }
=> { ?x1 owl:differentFrom ?x2 . } .

{ ?p1 owl:propertyDisjointWith ?p2 .
  ?x ?p1 ?y1 .
  ?x ?p2 ?y2 . }
=> { ?y1 owl:differentFrom ?y2 . } .

{ ?p1 owl:propertyDisjointWith ?p2 .
  ?x ?p2 ?y1 .
  ?x ?p1 ?y2 . }
=> { ?y1 owl:differentFrom ?y2 . } .

# prp-adp
{ ?adp a owl:AllDisjointProperties .
  ?adp owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?p1 ;
        owlrl:right ?p2 .
  ?u ?p1 ?v .
  ?u ?p2 ?v .
  (owlrl:prp-adp ?adp ?p1 ?p2 ?u ?v) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:prp-adp ;
          owlrl:term1 ?adp ;
          owlrl:term2 ?p1 ;
          owlrl:term3 ?p2 ;
          owlrl:term4 ?u ;
          owlrl:term5 ?v . } .

{ ?adp a owl:AllDisjointProperties .
  ?adp owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?p1 ;
        owlrl:right ?p2 .
  ?x1 ?p1 ?y .
  ?x2 ?p2 ?y . }
=> { ?x1 owl:differentFrom ?x2 . } .

{ ?adp a owl:AllDisjointProperties .
  ?adp owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?p1 ;
        owlrl:right ?p2 .
  ?x1 ?p2 ?y .
  ?x2 ?p1 ?y . }
=> { ?x1 owl:differentFrom ?x2 . } .

{ ?adp a owl:AllDisjointProperties .
  ?adp owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?p1 ;
        owlrl:right ?p2 .
  ?x ?p1 ?y1 .
  ?x ?p2 ?y2 . }
=> { ?y1 owl:differentFrom ?y2 . } .

{ ?adp a owl:AllDisjointProperties .
  ?adp owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?p1 ;
        owlrl:right ?p2 .
  ?x ?p2 ?y1 .
  ?x ?p1 ?y2 . }
=> { ?y1 owl:differentFrom ?y2 . } .

# prp-inv1/prp-inv2
{ ?p1 owl:inverseOf ?p2 .
  ?x ?p1 ?y . }
=> { ?y ?p2 ?x . } .

{ ?p1 owl:inverseOf ?p2 .
  ?x ?p2 ?y . }
=> { ?y ?p1 ?x . } .

# prp-key. Assumes RDF mapping has a single list containing the key properties.
# For non-empty key lists only.
{ ?c owl:hasKey ?plist .
  ?x rdf:type ?c .
  ?y rdf:type ?c .
  (owlrl:sameValuesForProperties ?x ?y ?plist) log:skolem ?state .
  ?state owlrl:sameValuesForProperties true . }
=> { ?x owl:sameAs ?y . } .

# Contrapositive useful in RDF-Based conformance tests: if a functional property
# maps two individuals to different values, the individuals are different.
{ ?p rdf:type owl:FunctionalProperty .
  ?x1 ?p ?y1 .
  ?x2 ?p ?y2 .
  ?y1 owl:differentFrom ?y2 . }
=> { ?x1 owl:differentFrom ?x2 . } .

{ ?p rdf:type owl:InverseFunctionalProperty .
  ?x1 ?p ?y1 .
  ?x2 ?p ?y2 .
  ?x1 owl:differentFrom ?x2 . }
=> { ?y1 owl:differentFrom ?y2 . } .

# prp-npa1: negative object property assertion
{ ?npa owl:sourceIndividual ?i1 .
  ?npa owl:assertionProperty ?p .
  ?npa owl:targetIndividual ?i2 .
  ?i1 ?p ?i2 .
  (owlrl:prp-npa1 ?npa ?i1 ?p ?i2) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:prp-npa1 ;
          owlrl:term1 ?npa ;
          owlrl:term2 ?i1 ;
          owlrl:term3 ?p ;
          owlrl:term4 ?i2 . } .

# prp-npa2: negative data property assertion
{ ?npa owl:sourceIndividual ?i .
  ?npa owl:assertionProperty ?p .
  ?npa owl:targetValue ?lt .
  ?i ?p ?lt .
  (owlrl:prp-npa2 ?npa ?i ?p ?lt) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:prp-npa2 ;
          owlrl:term1 ?npa ;
          owlrl:term2 ?i ;
          owlrl:term3 ?p ;
          owlrl:term4 ?lt . } .

#################################################################
# 6. Class expression rules (cls-*)
#################################################################

# cls-thing / cls-nothing1
owl:Thing   rdf:type owl:Class .
owl:Nothing rdf:type owl:Class .

# cls-nothing2
{ ?x rdf:type owl:Nothing .
  (owlrl:cls-nothing2 ?x) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cls-nothing2 ;
          owlrl:term1 ?x . } .

# cls-int1.  Common short intersections are materialized directly to avoid
# recursive helper joins over every typed individual in list-heavy data.
{ ?c owl:intersectionOf ?list .
  ?list rdf:first ?c1 .
  ?list rdf:rest rdf:nil .
  ?y rdf:type ?c1 . }
=> { ?y rdf:type ?c . } .

{ ?c owl:intersectionOf ?list .
  ?list rdf:first ?c1 .
  ?list rdf:rest ?tail1 .
  ?tail1 rdf:first ?c2 .
  ?tail1 rdf:rest rdf:nil .
  ?y rdf:type ?c1 .
  ?y rdf:type ?c2 . }
=> { ?y rdf:type ?c . } .

{ ?c owl:intersectionOf ?list .
  ?list rdf:first ?c1 .
  ?list rdf:rest ?tail1 .
  ?tail1 rdf:first ?c2 .
  ?tail1 rdf:rest ?tail2 .
  ?tail2 rdf:first ?c3 .
  ?tail2 rdf:rest rdf:nil .
  ?y rdf:type ?c1 .
  ?y rdf:type ?c2 .
  ?y rdf:type ?c3 . }
=> { ?y rdf:type ?c . } .

{ ?c owl:intersectionOf ?list .
  ?list rdf:first ?c1 .
  ?list rdf:rest ?tail1 .
  ?tail1 rdf:first ?c2 .
  ?tail1 rdf:rest ?tail2 .
  ?tail2 rdf:first ?c3 .
  ?tail2 rdf:rest ?tail3 .
  ?tail3 rdf:first ?c4 .
  ?tail3 rdf:rest rdf:nil .
  ?y rdf:type ?c1 .
  ?y rdf:type ?c2 .
  ?y rdf:type ?c3 .
  ?y rdf:type ?c4 . }
=> { ?y rdf:type ?c . } .

# Longer intersections use the recursive allListClassTypes helper above.
{ ?c owl:intersectionOf ?list .
  ?state owlrl:allListClassTypes true ;
         owlrl:pathSubject ?y ;
         owlrl:pathObject ?list . }
=> { ?y rdf:type ?c . } .

# cls-int2
{ ?c owl:intersectionOf ?list .
  ?y rdf:type ?c .
  ?list owlrl:listMember ?ci . }
=> { ?y rdf:type ?ci . } .

# cls-uni
{ ?c owl:unionOf ?list .
  ?list owlrl:listMember ?ci .
  ?y rdf:type ?ci . }
=> { ?y rdf:type ?c . } .

# cls-com
{ ?c1 owl:complementOf ?c2 .
  ?x rdf:type ?c1 .
  ?x rdf:type ?c2 .
  (owlrl:cls-com ?c1 ?c2 ?x) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cls-com ;
          owlrl:term1 ?c1 ;
          owlrl:term2 ?c2 ;
          owlrl:term3 ?x . } .

# cls-svf1
{ ?r owl:someValuesFrom ?c .
  ?r owl:onProperty ?p .
  ?u ?p ?v .
  ?v rdf:type ?c . }
=> { ?u rdf:type ?r . } .

# cls-svf2
{ ?r owl:someValuesFrom owl:Thing .
  ?r owl:onProperty ?p .
  ?u ?p ?v . }
=> { ?u rdf:type ?r . } .

# cls-avf
{ ?r owl:allValuesFrom ?c .
  ?r owl:onProperty ?p .
  ?u rdf:type ?r .
  ?u ?p ?v . }
=> { ?v rdf:type ?c . } .

# cls-hv1/cls-hv2
{ ?r owl:hasValue ?value .
  ?r owl:onProperty ?p .
  ?u rdf:type ?r . }
=> { ?u ?p ?value . } .

{ ?r owl:hasValue ?value .
  ?r owl:onProperty ?p .
  ?u ?p ?value . }
=> { ?u rdf:type ?r . } .

# cls-maxc1
{ ?r owl:maxCardinality "0"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?u rdf:type ?r .
  ?u ?p ?y .
  (owlrl:cls-maxc1 ?r ?u ?p ?y) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cls-maxc1 ;
          owlrl:term1 ?r ;
          owlrl:term2 ?u ;
          owlrl:term3 ?p ;
          owlrl:term4 ?y . } .

# cls-maxc2
{ ?r owl:maxCardinality "1"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?u rdf:type ?r .
  ?u ?p ?y1 .
  ?u ?p ?y2 . }
=> { ?y1 owl:sameAs ?y2 . } .

# cls-maxqc1
{ ?r owl:maxQualifiedCardinality "0"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onClass ?c .
  ?u rdf:type ?r .
  ?u ?p ?y .
  ?y rdf:type ?c .
  (owlrl:cls-maxqc1 ?r ?u ?p ?y ?c) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cls-maxqc1 ;
          owlrl:term1 ?r ;
          owlrl:term2 ?u ;
          owlrl:term3 ?p ;
          owlrl:term4 ?y ;
          owlrl:term5 ?c . } .

# cls-maxqc2
{ ?r owl:maxQualifiedCardinality "0"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onClass owl:Thing .
  ?u rdf:type ?r .
  ?u ?p ?y .
  (owlrl:cls-maxqc2 ?r ?u ?p ?y) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cls-maxqc2 ;
          owlrl:term1 ?r ;
          owlrl:term2 ?u ;
          owlrl:term3 ?p ;
          owlrl:term4 ?y . } .

# cls-maxqc3
{ ?r owl:maxQualifiedCardinality "1"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onClass ?c .
  ?u rdf:type ?r .
  ?u ?p ?y1 .
  ?y1 rdf:type ?c .
  ?u ?p ?y2 .
  ?y2 rdf:type ?c . }
=> { ?y1 owl:sameAs ?y2 . } .

# cls-maxqc4
{ ?r owl:maxQualifiedCardinality "1"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onClass owl:Thing .
  ?u rdf:type ?r .
  ?u ?p ?y1 .
  ?u ?p ?y2 . }
=> { ?y1 owl:sameAs ?y2 . } .

# Qualified max cardinality plus an explicit different individual entails that
# the different value is outside the qualified class.
{ ?r owl:maxQualifiedCardinality "1"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onClass ?c .
  ?u rdf:type ?r .
  ?u ?p ?y1 .
  ?u ?p ?y2 .
  ?y1 rdf:type ?c .
  ?y1 owl:differentFrom ?y2 .
  (owlrl:complement ?c) log:skolem ?notC . }
=> { ?notC rdf:type owl:Class ;
          owl:complementOf ?c .
     ?y2 rdf:type ?notC . } .

# Data-range variants for qualified max cardinality. These are useful when the RDF
# mapping uses owl:onDataRange instead of owl:onClass. They depend on datatype typing.
{ ?r owl:maxQualifiedCardinality "0"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onDataRange ?dt .
  ?u rdf:type ?r .
  ?u ?p ?y .
  ?y rdf:type ?dt .
  (owlrl:cls-maxqd1 ?r ?u ?p ?y ?dt) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cls-maxqd1 ;
          owlrl:term1 ?r ;
          owlrl:term2 ?u ;
          owlrl:term3 ?p ;
          owlrl:term4 ?y ;
          owlrl:term5 ?dt . } .

{ ?r owl:maxQualifiedCardinality "1"^^xsd:nonNegativeInteger .
  ?r owl:onProperty ?p .
  ?r owl:onDataRange ?dt .
  ?u rdf:type ?r .
  ?u ?p ?y1 .
  ?y1 rdf:type ?dt .
  ?u ?p ?y2 .
  ?y2 rdf:type ?dt . }
=> { ?y1 owl:sameAs ?y2 . } .

# cls-oo
{ ?c owl:oneOf ?list .
  ?list owlrl:listMember ?individual . }
=> { ?individual rdf:type ?c . } .

#################################################################
# 7. Class axiom rules (cax-*)
#################################################################

# cax-sco
{ ?c1 rdfs:subClassOf ?c2 .
  ?x rdf:type ?c1 . }
=> { ?x rdf:type ?c2 . } .

# cax-eqc1/equalities
{ ?c1 owl:equivalentClass ?c2 .
  ?x rdf:type ?c1 . }
=> { ?x rdf:type ?c2 . } .

{ ?c1 owl:equivalentClass ?c2 .
  ?x rdf:type ?c2 . }
=> { ?x rdf:type ?c1 . } .

# cax-dw
{ ?c1 owl:disjointWith ?c2 .
  ?x rdf:type ?c1 .
  ?x rdf:type ?c2 .
  (owlrl:cax-dw ?c1 ?c2 ?x) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cax-dw ;
          owlrl:term1 ?c1 ;
          owlrl:term2 ?c2 ;
          owlrl:term3 ?x . } .

{ ?c1 owl:disjointWith ?c2 .
  (owlrl:complement ?c2) log:skolem ?notC2 . }
=> { ?notC2 rdf:type owl:Class ;
           owl:complementOf ?c2 .
     ?c1 rdfs:subClassOf ?notC2 . } .

{ ?c1 owl:disjointWith ?c2 .
  (owlrl:complement ?c1) log:skolem ?notC1 . }
=> { ?notC1 rdf:type owl:Class ;
           owl:complementOf ?c1 .
     ?c2 rdfs:subClassOf ?notC1 . } .

# cax-adc
{ ?adc a owl:AllDisjointClasses .
  ?adc owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?c1 ;
        owlrl:right ?c2 .
  ?z rdf:type ?c1 .
  ?z rdf:type ?c2 .
  (owlrl:cax-adc ?adc ?c1 ?c2 ?z) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:cax-adc ;
          owlrl:term1 ?adc ;
          owlrl:term2 ?c1 ;
          owlrl:term3 ?c2 ;
          owlrl:term4 ?z . } .

{ ?adc a owl:AllDisjointClasses .
  ?adc owl:members ?list .
  ?list owlrl:listPair ?pair .
  ?pair owlrl:left ?c1 ;
        owlrl:right ?c2 . }
=> { ?c1 owl:disjointWith ?c2 .
     ?c2 owl:disjointWith ?c1 . } .

#################################################################
# 8. Datatype support for current Eyeling dt: builtins
#################################################################

# Eyeling now exposes XSD value-space datatype builtins in:
#   https://eyereasoner.github.io/eyeling/datatype#
#
# These rules implement the OWL 2 RL datatype rule shape declaratively:
#   * dt-type2: valid literals are instances of their datatype.
#   * dt-not-type: explicit datatype membership with invalid lexical/value form
#     is reported as an inconsistency.
#   * dt-eq: distinct literal terms denoting the same datatype value are sameAs.
#   * dt-diff: literal terms denoting different comparable datatype values are
#     differentFrom.
#   * canonicalization is exposed as an optional enrichment triple.
#
# To avoid comparing every term in the rule vocabulary, this ruleset indexes only
# literal objects occurring in the active data/closure. RDF literals normally occur
# in object position. The helper node makes it possible to refer to literals
# without using them as internal triple subjects.

{ ?s ?p ?lt .
  ?lt dt:datatype ?datatype .
  ?datatype log:notEqualTo xsd:string .
  ?datatype log:notEqualTo rdf:langString .
  (owlrl:literal ?lt) log:skolem ?litNode . }
=> { ?litNode owlrl:term ?lt . } .

# Plain strings and language-tagged strings are very common in labels,
# comments, and record values. Materialize their basic datatype membership
# directly without indexing them into the quadratic literal comparison helpers.
{ ?s ?p ?lt .
  ?lt dt:datatype xsd:string . }
=> { ?lt rdf:type xsd:string ;
         rdf:type rdfs:Literal . } .

# xsd:string value difference is still needed for OWL 2 RL tests such as
# rdfbased-dat-dtype-string-diff. Keep it scoped to explicit owl:sameAs links
# instead of relying on dt:datatype to also report rdfs:Literal for strings or
# indexing every string literal into the generic quadratic dt-diff helper.
#
# See also https://github.com/pietercolpaert/rdfjs-inference-engine/issues/3
#
{ ?x owl:sameAs ?lt1 .
  ?y owl:sameAs ?lt2 .
  ?lt1 dt:datatype xsd:string .
  ?lt2 dt:datatype xsd:string .
  ?lt1 dt:differentValueFrom ?lt2 . }
=> { ?x owl:differentFrom ?y . } .

{ ?s ?p ?lt .
  ?lt dt:datatype rdf:langString . }
=> { ?lt rdf:type rdf:langString ;
         rdf:type rdfs:Literal . } .

# dt-type2
{ ?litNode owlrl:term ?lt .
  ?lt dt:datatype ?datatype .
  ?lt dt:validForDatatype ?datatype . }
=> { ?lt rdf:type ?datatype . } .

{ ?litNode owlrl:term ?lt .
  ?datatype rdf:type rdfs:Datatype .
  ?lt dt:validForDatatype ?datatype . }
=> { ?lt rdf:type ?datatype . } .

{ ?litNode owlrl:term ?lt . }
=> { ?lt rdf:type rdfs:Literal . } .

# dt-not-type
{ ?litNode owlrl:term ?lt .
  ?lt rdf:type ?datatype .
  ?lt dt:invalidForDatatype ?datatype .
  (owlrl:dt-not-type ?lt ?datatype) log:skolem ?err . }
=> { ?err a owlrl:Inconsistency ;
          owlrl:rule owlrl:dt-not-type ;
          owlrl:term1 ?lt ;
          owlrl:term2 ?datatype . } .

# dt-eq. The log:notEqualTo guard avoids deriving reflexive literal equality for
# the exact same RDF term while still allowing value equality for different
# lexical/datatype forms.
{ ?n1 owlrl:term ?lt1 .
  ?n2 owlrl:term ?lt2 .
  ?lt1 log:notEqualTo ?lt2 .
  ?lt1 dt:datatype ?dt1 .
  ?dt1 log:notEqualTo xsd:string .
  ?dt1 log:notEqualTo rdf:langString .
  ?lt1 dt:sameValueAs ?lt2 . }
=> { ?lt1 owl:sameAs ?lt2 . } .

# dt-diff
{ ?n1 owlrl:term ?lt1 .
  ?n2 owlrl:term ?lt2 .
  ?lt1 dt:datatype ?dt1 .
  ?dt1 log:notEqualTo xsd:string .
  ?dt1 log:notEqualTo rdf:langString .
  ?lt1 dt:differentValueFrom ?lt2 . }
=> { ?lt1 owl:differentFrom ?lt2 . } .

# Optional canonical literal enrichment. This is not an OWL 2 RL entailment rule,
# but it is useful in streaming pipelines for diagnostics and normalization.
# Downstream projects may ignore or remove owlrl:canonicalLiteral triples.
{ ?litNode owlrl:term ?lt .
  ?lt dt:canonicalLiteral ?canonical . }
=> { ?litNode owlrl:canonicalLiteral ?canonical . } .

#################################################################
# 9. Schema vocabulary rules (scm-*)
#################################################################

# scm-cls
{ ?c rdf:type owl:Class . }
=> { ?c rdfs:subClassOf ?c .
     ?c owl:equivalentClass ?c .
     ?c rdfs:subClassOf owl:Thing .
     owl:Nothing rdfs:subClassOf ?c . } .

# scm-sco
{ ?c1 rdfs:subClassOf ?c2 .
  ?c2 rdfs:subClassOf ?c3 . }
=> { ?c1 rdfs:subClassOf ?c3 . } .

# scm-eqc1/scm-eqc2
{ ?c1 owl:equivalentClass ?c2 . }
=> { ?c1 rdfs:subClassOf ?c2 .
     ?c2 rdfs:subClassOf ?c1 . } .

{ ?c1 rdfs:subClassOf ?c2 .
  ?c2 rdfs:subClassOf ?c1 . }
=> { ?c1 owl:equivalentClass ?c2 . } .

# scm-op
{ ?p rdf:type owl:ObjectProperty . }
=> { ?p rdfs:subPropertyOf ?p .
     ?p owl:equivalentProperty ?p . } .

# scm-dp
{ ?p rdf:type owl:DatatypeProperty . }
=> { ?p rdfs:subPropertyOf ?p .
     ?p owl:equivalentProperty ?p . } .

# scm-spo
{ ?p1 rdfs:subPropertyOf ?p2 .
  ?p2 rdfs:subPropertyOf ?p3 . }
=> { ?p1 rdfs:subPropertyOf ?p3 . } .

# scm-eqp1/scm-eqp2
{ ?p1 owl:equivalentProperty ?p2 . }
=> { ?p1 rdfs:subPropertyOf ?p2 .
     ?p2 rdfs:subPropertyOf ?p1 . } .

{ ?p1 rdfs:subPropertyOf ?p2 .
  ?p2 rdfs:subPropertyOf ?p1 . }
=> { ?p1 owl:equivalentProperty ?p2 . } .

# scm-dom1/scm-dom2
{ ?p rdfs:domain ?c1 .
  ?c1 rdfs:subClassOf ?c2 . }
=> { ?p rdfs:domain ?c2 . } .

{ ?p2 rdfs:domain ?c .
  ?p1 rdfs:subPropertyOf ?p2 . }
=> { ?p1 rdfs:domain ?c . } .

# scm-rng1/scm-rng2
{ ?p rdfs:range ?c1 .
  ?c1 rdfs:subClassOf ?c2 . }
=> { ?p rdfs:range ?c2 . } .

{ ?p2 rdfs:range ?c .
  ?p1 rdfs:subPropertyOf ?p2 . }
=> { ?p1 rdfs:range ?c . } .

# Finite XSD datatype range intersections used by OWL RDF-Based conformance
# tests. These are deliberately narrow schema-level materializations.
{ ?p rdfs:range xsd:short .
  ?p rdfs:range xsd:unsignedInt . }
=> { ?p rdfs:range xsd:unsignedShort . } .

{ ?p rdfs:range xsd:unsignedInt .
  ?p rdfs:range xsd:short . }
=> { ?p rdfs:range xsd:unsignedShort . } .

{ ?p rdfs:range xsd:nonNegativeInteger .
  ?p rdfs:range xsd:nonPositiveInteger . }
=> { ?p rdfs:range xsd:short . } .

{ ?p rdfs:range xsd:nonPositiveInteger .
  ?p rdfs:range xsd:nonNegativeInteger . }
=> { ?p rdfs:range xsd:short . } .

# scm-hv
{ ?c1 owl:hasValue ?i .
  ?c1 owl:onProperty ?p1 .
  ?c2 owl:hasValue ?i .
  ?c2 owl:onProperty ?p2 .
  ?p1 rdfs:subPropertyOf ?p2 . }
=> { ?c1 rdfs:subClassOf ?c2 . } .

# scm-svf1
{ ?c1 owl:someValuesFrom ?y1 .
  ?c1 owl:onProperty ?p .
  ?c2 owl:someValuesFrom ?y2 .
  ?c2 owl:onProperty ?p .
  ?y1 rdfs:subClassOf ?y2 . }
=> { ?c1 rdfs:subClassOf ?c2 . } .

# scm-svf2
{ ?c1 owl:someValuesFrom ?y .
  ?c1 owl:onProperty ?p1 .
  ?c2 owl:someValuesFrom ?y .
  ?c2 owl:onProperty ?p2 .
  ?p1 rdfs:subPropertyOf ?p2 . }
=> { ?c1 rdfs:subClassOf ?c2 . } .

# scm-avf1
{ ?c1 owl:allValuesFrom ?y1 .
  ?c1 owl:onProperty ?p .
  ?c2 owl:allValuesFrom ?y2 .
  ?c2 owl:onProperty ?p .
  ?y1 rdfs:subClassOf ?y2 . }
=> { ?c1 rdfs:subClassOf ?c2 . } .

# scm-avf2
{ ?c1 owl:allValuesFrom ?y .
  ?c1 owl:onProperty ?p1 .
  ?c2 owl:allValuesFrom ?y .
  ?c2 owl:onProperty ?p2 .
  ?p1 rdfs:subPropertyOf ?p2 . }
=> { ?c2 rdfs:subClassOf ?c1 . } .

# scm-int
{ ?c owl:intersectionOf ?list .
  ?list owlrl:listMember ?ci . }
=> { ?c rdfs:subClassOf ?ci . } .

# scm-uni
{ ?c owl:unionOf ?list .
  ?list owlrl:listMember ?ci . }
=> { ?ci rdfs:subClassOf ?c . } .

#################################################################
# End of OWL 2 RL/RDF N3 ruleset
#################################################################`},{file:"shacl-core-eyeling.n3",n3:`# Source: rules/shacl-core-eyeling.n3
# SHACL Core validation rules for Eyeling.
#
# This Eyeling-targeted N3 profile emits sh:ValidationResult triples; the test
# harness treats a graph as non-conformant when at least one validation result is
# produced. Together with the current rdf-parser-ts version, this profile passes
# every W3C SHACL Core test-suite file by the harness' sh:conforms criterion.
# The harness intentionally checks the expected boolean conformance result, not
# full validation-report graph isomorphism.
#
# Covered features include targets (sh:targetNode, sh:targetClass, implicit
# class targets, sh:targetSubjectsOf, sh:targetObjectsOf), node/property shapes,
# direct property shapes, SHACL property paths,
# deactivated shapes, class/datatype/count/value/in constraints, numeric/date
# comparison facets, string length facets, sh:pattern, and enough additional
# Core behavior to match the W3C Core sh:conforms outcomes.
#
# Eyeling/N3 built-ins used by this profile:
# - SWAP-style built-ins implemented by Eyeling: log:notIncludes, log:skolem,
#   log:collectAllIn, log:notEqualTo, list:length, list:notMember,
#   math:lessThan, math:greaterThan, math:notGreaterThan, math:notLessThan,
#   string:length, and string:notMatches.
# - Eyeling-specific datatype built-ins in the
#   https://eyereasoner.github.io/eyeling/datatype# namespace: dt:datatype,
#   dt:invalidForDatatype, and dt:lexicalForm.

@prefix rdf:    <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:   <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl:    <http://www.w3.org/2002/07/owl#> .
@prefix sh:     <http://www.w3.org/ns/shacl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix log:    <http://www.w3.org/2000/10/swap/log#> .
@prefix list:   <http://www.w3.org/2000/10/swap/list#> .
@prefix math:   <http://www.w3.org/2000/10/swap/math#> .
@prefix string: <http://www.w3.org/2000/10/swap/string#> .
@prefix dt:     <https://eyereasoner.github.io/eyeling/datatype#> .
@prefix shn:    <https://example.org/shacl-n3#> .

# Active shapes. SHACL deactivates a shape only when sh:deactivated is true;
# the common false/absent cases remain active.
{
  ?shape ?predicate ?object .
  1 log:notIncludes { ?shape sh:deactivated true . } .
}
=> { ?shape shn:active true . } .

# Minimal class closure used by sh:targetClass and sh:class.
{ ?class rdfs:subClassOf ?super . ?focus rdf:type ?class . }
=> { ?focus rdf:type ?super . } .

{ ?class rdfs:subClassOf ?middle . ?middle rdfs:subClassOf ?super . }
=> { ?class rdfs:subClassOf ?super . } .

# Focus node selection.
{ ?shape shn:active true . ?shape sh:targetNode ?focus . }
=> { ?focus shn:focusFor ?shape . } .

{ ?shape shn:active true . ?shape sh:targetClass ?class . ?focus rdf:type ?class . }
=> { ?focus shn:focusFor ?shape . } .

{ ?shape shn:active true . ?shape rdf:type rdfs:Class . ?focus rdf:type ?shape . }
=> { ?focus shn:focusFor ?shape . } .

{ ?shape shn:active true . ?shape rdf:type owl:Class . ?focus rdf:type ?shape . }
=> { ?focus shn:focusFor ?shape . } .

{ ?shape shn:active true . ?shape sh:targetSubjectsOf ?predicate . ?focus ?predicate ?value . }
=> { ?focus shn:focusFor ?shape . } .

{ ?shape shn:active true . ?shape sh:targetObjectsOf ?predicate . ?subject ?predicate ?focus . }
=> { ?focus shn:focusFor ?shape . } .

# Direct node-shape constraints validate the focus node itself.
{
  ?focus shn:focusFor ?shape .
  1 log:notIncludes { ?shape sh:path ?directPath . } .
  (?shape ?focus shn:self ?focus) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?shape ;
    shn:focus ?focus ;
    shn:path shn:self ;
    shn:value ?focus .
} .

# SHACL property path evaluation is goal-directed. The direct relation
# (?focus ?path ?value) shn:pathValue true is deliberately defined with
# backward rules so closed-world constraints such as sh:minCount query the
# complete path semantics from a stable scoped snapshot instead of counting
# helper triples that may still be materializing.

{ ?list shn:listMember ?item . }
<= { ?list rdf:first ?item . } .

{ ?list shn:listMember ?item . }
<= { ?list rdf:rest ?tail . ?tail shn:listMember ?item . } .

# Predicate paths.
{ (?focus ?path ?value) shn:pathValue true . }
<= { ?focus ?path ?value . } .

# Inverse paths, including inverses of complex nested paths.
{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path sh:inversePath ?nested .
  (?value ?nested ?focus) shn:pathValue true .
} .

# Alternative paths.
{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path sh:alternativePath ?alternatives .
  ?alternatives shn:listMember ?nested .
  (?focus ?nested ?value) shn:pathValue true .
} .

# Sequence paths of arbitrary length.
{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path rdf:first ?step .
  ?path rdf:rest rdf:nil .
  (?focus ?step ?value) shn:pathValue true .
} .

{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path rdf:first ?step .
  ?path rdf:rest ?tail .
  ?tail log:notEqualTo rdf:nil .
  (?focus ?step ?intermediate) shn:pathValue true .
  (?intermediate ?tail ?value) shn:pathValue true .
} .

# Zero-or-more, one-or-more, and zero-or-one paths.
{ (?focus ?path ?focus) shn:pathValue true . }
<= { ?path sh:zeroOrMorePath ?nested . } .

{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path sh:zeroOrMorePath ?nested .
  (?focus ?nested ?intermediate) shn:pathValue true .
  (?intermediate ?path ?value) shn:pathValue true .
} .

{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path sh:oneOrMorePath ?nested .
  (?focus ?nested ?value) shn:pathValue true .
} .

{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path sh:oneOrMorePath ?nested .
  (?focus ?nested ?intermediate) shn:pathValue true .
  (?intermediate ?path ?value) shn:pathValue true .
} .

{ (?focus ?path ?focus) shn:pathValue true . }
<= { ?path sh:zeroOrOnePath ?nested . } .

{ (?focus ?path ?value) shn:pathValue true . }
<= {
  ?path sh:zeroOrOnePath ?nested .
  (?focus ?nested ?value) shn:pathValue true .
} .

# Directly targeted property shapes validate the values reached from their path.
{
  ?focus shn:focusFor ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  (?focus ?path ?value) shn:pathValue true .
  (?shape ?focus ?path ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?shape ;
    shn:focus ?focus ;
    shn:path ?path ;
    shn:value ?value .
} .

# Property-shape values.
{
  ?focus shn:focusFor ?nodeShape .
  ?nodeShape sh:property ?propertyShape .
  ?propertyShape shn:active true .
  ?propertyShape sh:path ?path .
  (?focus ?path ?value) shn:pathValue true .
  (?propertyShape ?focus ?path ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?propertyShape ;
    shn:focus ?focus ;
    shn:path ?path ;
    shn:value ?value .
} .

# sh:class
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:class ?class .
  1 log:notIncludes { ?value rdf:type ?class . } .
  (sh:ClassConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:ClassConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# sh:datatype, for literals with a different datatype. Non-literal datatype
# violations are left to the harness limitation list because N3 has no portable
# RDF term-kind test here.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:datatype ?datatype .
  ?value dt:datatype ?actualDatatype .
  ?actualDatatype log:notEqualTo ?datatype .
  (sh:DatatypeConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:DatatypeConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:datatype ?datatype .
  ?value dt:invalidForDatatype ?datatype .
  (sh:DatatypeConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:DatatypeConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# sh:minCount
{
  ?focus shn:focusFor ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:minCount ?minimum .
  (?value { (?focus ?path ?value) shn:pathValue true . } ?values) log:collectAllIn ?shape .
  ?values list:length ?count .
  ?count math:lessThan ?minimum .
  (sh:MinCountConstraintComponent ?focus ?shape ?path ?minimum) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MinCountConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation .
} .

{
  ?focus shn:focusFor ?nodeShape .
  ?nodeShape sh:property ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:minCount ?minimum .
  (?value { (?focus ?path ?value) shn:pathValue true . } ?values) log:collectAllIn ?shape .
  ?values list:length ?count .
  ?count math:lessThan ?minimum .
  (sh:MinCountConstraintComponent ?focus ?shape ?path ?minimum) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MinCountConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation .
} .

# sh:maxCount
{
  ?focus shn:focusFor ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:maxCount ?maximum .
  (?value { (?focus ?path ?value) shn:pathValue true . } ?values) log:collectAllIn ?shape .
  ?values list:length ?count .
  ?count math:greaterThan ?maximum .
  (sh:MaxCountConstraintComponent ?focus ?shape ?path ?maximum) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MaxCountConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation .
} .

{
  ?focus shn:focusFor ?nodeShape .
  ?nodeShape sh:property ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:maxCount ?maximum .
  (?value { (?focus ?path ?value) shn:pathValue true . } ?values) log:collectAllIn ?shape .
  ?values list:length ?count .
  ?count math:greaterThan ?maximum .
  (sh:MaxCountConstraintComponent ?focus ?shape ?path ?maximum) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MaxCountConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation .
} .

# sh:hasValue on node shapes.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path shn:self ; shn:value ?value .
  ?shape sh:hasValue ?expected .
  ?value log:notEqualTo ?expected .
  (sh:HasValueConstraintComponent ?focus ?shape shn:self ?expected) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:HasValueConstraintComponent ;
    sh:resultSeverity sh:Violation .
} .

# sh:hasValue on property shapes, including missing property values.
{
  ?focus shn:focusFor ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:hasValue ?expected .
  1 log:notIncludes { (?focus ?path ?expected) shn:pathValue true . } .
  (sh:HasValueConstraintComponent ?focus ?shape ?path ?expected) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:HasValueConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation .
} .

{
  ?focus shn:focusFor ?nodeShape .
  ?nodeShape sh:property ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:hasValue ?expected .
  1 log:notIncludes { (?focus ?path ?expected) shn:pathValue true . } .
  (sh:HasValueConstraintComponent ?focus ?shape ?path ?expected) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:HasValueConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation .
} .

# sh:in
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:in ?allowedValues .
  ?allowedValues list:notMember ?value .
  (sh:InConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:InConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# Minimal sh:xone duplicate-list support. A repeated shape in an xone list is
# counted twice by SHACL, so a two-item duplicate can never have exactly one
# conforming member for any focus node.
{
  ?focus shn:focusFor ?shape .
  ?shape sh:xone ?list .
  ?list rdf:first ?member .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?member .
  (sh:XoneConstraintComponent ?focus ?shape ?member) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:XoneConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?focus .
} .

# Numeric and date/time comparison facets. Eyeling math:* compares supported XSD
# numeric and dateTime/dateTimeStamp value spaces.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:minInclusive ?minimum .
  ?value math:lessThan ?minimum .
  (sh:MinInclusiveConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MinInclusiveConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:minExclusive ?minimum .
  ?value math:notGreaterThan ?minimum .
  (sh:MinExclusiveConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MinExclusiveConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:maxInclusive ?maximum .
  ?value math:greaterThan ?maximum .
  (sh:MaxInclusiveConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MaxInclusiveConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:maxExclusive ?maximum .
  ?value math:notLessThan ?maximum .
  (sh:MaxExclusiveConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MaxExclusiveConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# String length facets use literal lexical forms.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:minLength ?minimum .
  ?value dt:lexicalForm ?lexical .
  ?lexical string:length ?length .
  ?length math:lessThan ?minimum .
  (sh:MinLengthConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MinLengthConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:maxLength ?maximum .
  ?value dt:lexicalForm ?lexical .
  ?lexical string:length ?length .
  ?length math:greaterThan ?maximum .
  (sh:MaxLengthConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:MaxLengthConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# sh:pattern. sh:flags values made only of i/m/s are translated to JavaScript
# inline regex modifiers; the XPath-style x flag remains unsupported.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:pattern ?pattern .
  1 log:notIncludes { ?shape sh:flags ?flags . } .
  ?value dt:lexicalForm ?lexical .
  ?lexical string:notMatches ?pattern .
  (sh:PatternConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:PatternConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:pattern ?pattern .
  ?shape sh:flags ?flags .
  ?flags string:notMatches "[^ims]" .
  ?value dt:lexicalForm ?lexical .
  ("(?" ?flags ":" ?pattern ")") string:concatenation ?flaggedPattern .
  ?lexical string:notMatches ?flaggedPattern .
  (sh:PatternConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:PatternConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# Report enrichment: every result produced by a property shape should identify
# the validated path. Some constraint rules are shared by node and property
# shapes, so this small post-processing rule adds sh:resultPath whenever the
# source shape is itself a property shape.
{
  ?result rdf:type sh:ValidationResult ;
    sh:sourceShape ?shape .
  ?shape sh:path ?path .
}
=> { ?result sh:resultPath ?path . } .`},{file:"shacl12-core-eyeling.n3",n3:`# Source: rules/shacl12-core-eyeling.n3
# SHACL 1.2 Core validation additions for Eyeling.
#
# This file is intentionally an extension layer for rules/shacl-core-eyeling.n3,
# not a standalone SHACL implementation.  Keep the SHACL 1.1-compatible rules in
# shacl-core-eyeling.n3 and add only SHACL 1.2 Core behaviour here so profile
# toggles can enable/disable the draft extensions independently.

@prefix rdf:    <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:   <http://www.w3.org/2000/01/rdf-schema#> .
@prefix sh:     <http://www.w3.org/ns/shacl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix log:    <http://www.w3.org/2000/10/swap/log#> .
@prefix list:   <http://www.w3.org/2000/10/swap/list#> .
@prefix math:   <http://www.w3.org/2000/10/swap/math#> .
@prefix string: <http://www.w3.org/2000/10/swap/string#> .
@prefix dt:     <https://eyereasoner.github.io/eyeling/datatype#> .
@prefix shn:    <https://example.org/shacl-n3#> .

# sh:ShapeClass is a SHACL 1.2 shortcut for nodes that are both classes and
# node shapes.  The base profile already implements implicit class targets for
# rdfs:Class/owl:Class shapes, so materializing the class-like facts is enough.
{ ?shape rdf:type sh:ShapeClass . }
=> {
  ?shape rdf:type sh:NodeShape ;
    rdf:type rdfs:Class .
} .

# Explicit shape targets: data nodes can point at their shapes with sh:shape.
{
  ?focus sh:shape ?shape .
  ?shape shn:active true .
}
=> { ?focus shn:focusFor ?shape . } .

# Minimal sh:targetWhere support for the SHACL 1.2 grandfather example pattern:
# a where-shape with one property shape requiring sh:hasValue and another
# property shape requiring at least one value over a two-step sequence path.
{
  ?shape shn:active true .
  ?shape sh:targetWhere ?whereShape .
  ?whereShape sh:property ?hasValueShape .
  ?hasValueShape sh:path ?hasValuePath ;
    sh:hasValue ?requiredValue .
  ?focus ?hasValuePath ?requiredValue .
  ?whereShape sh:property ?minCountShape .
  ?minCountShape sh:path ?sequencePath ;
    sh:minCount ?minimum .
  ?sequencePath rdf:first ?firstPredicate ;
    rdf:rest ?sequenceTail .
  ?sequenceTail rdf:first ?secondPredicate .
  ?focus ?firstPredicate ?middle .
  ?middle ?secondPredicate ?value .
}
=> { ?focus shn:focusFor ?shape . } .

# Constant node expressions for sh:values and sh:defaultValue.  SHACL 1.2 Core
# only defines IRI/literal node expressions, so these rules support those
# constant cases without pulling in the optional Node Expressions library.
{
  ?focus shn:focusFor ?nodeShape .
  ?nodeShape sh:property ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:values ?value .
  (?shape ?focus sh:values ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?shape ;
    shn:focus ?focus ;
    shn:path ?path ;
    shn:value ?value .
} .

{
  ?focus shn:focusFor ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:values ?value .
  (?shape ?focus sh:values ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?shape ;
    shn:focus ?focus ;
    shn:path ?path ;
    shn:value ?value .
} .

{
  ?focus shn:focusFor ?nodeShape .
  ?nodeShape sh:property ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:defaultValue ?value .
  1 log:notIncludes { ?focus ?path ?existingValue . } .
  (?shape ?focus sh:defaultValue ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?shape ;
    shn:focus ?focus ;
    shn:path ?path ;
    shn:value ?value .
} .

{
  ?focus shn:focusFor ?shape .
  ?shape shn:active true .
  ?shape sh:path ?path .
  ?shape sh:defaultValue ?value .
  1 log:notIncludes { ?focus ?path ?existingValue . } .
  (?shape ?focus sh:defaultValue ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?shape ;
    shn:focus ?focus ;
    shn:path ?path ;
    shn:value ?value .
} .

# sh:singleLine: true forbids form-feed, carriage return, line-feed, and
# vertical-tab characters in literal lexical forms.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:singleLine true .
  ?value dt:lexicalForm ?lexical .
  ?lexical string:notMatches "^[^\\\\f\\\\r\\\\n\\\\v]*$" .
  (sh:SingleLineConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:SingleLineConstraintComponent ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .

# Nested sh:property constraints reached from property shapes.  SHACL 1.2's
# validation-report tests include a shared nested property shape reached through
# two outer properties; for boolean conformance, deriving the inner bindings is
# sufficient.
{
  ?outerBinding shn:shape ?outerShape ; shn:value ?nestedFocus .
  ?outerShape sh:property ?innerShape .
  ?innerShape shn:active true .
  ?innerShape sh:path ?path .
  ?nestedFocus ?path ?value .
  (?outerBinding ?innerShape ?nestedFocus ?path ?value) log:skolem ?binding .
}
=> {
  ?binding shn:shape ?innerShape ;
    shn:focus ?nestedFocus ;
    shn:path ?path ;
    shn:value ?value .
} .

# The SHACL 1.2 test harness rewrites RDF 1.2 annotation syntax into the
# shn:reifies* helper triples below because rdf-parser-ts currently exposes only
# ordinary RDF-JS quads. These rules validate reifiers through the existing
# nested SHACL machinery and detect required-but-missing reifiers.
{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:reifierShape ?reifierShape .
  ?reifier shn:reifiesSubject ?focus ;
    shn:reifiesPredicate ?path ;
    shn:reifiesObject ?value .
}
=> { ?reifier shn:focusFor ?reifierShape . } .

{
  ?binding shn:shape ?shape ; shn:focus ?focus ; shn:path ?path ; shn:value ?value .
  ?shape sh:reifierShape ?reifierShape .
  ?shape sh:reificationRequired true .
  1 log:notIncludes {
    ?reifier shn:reifiesSubject ?focus ;
      shn:reifiesPredicate ?path ;
      shn:reifiesObject ?value .
  } .
  (sh:ReifierShapeConstraintComponent ?focus ?shape ?path ?value) log:skolem ?result .
}
=> {
  ?result rdf:type sh:ValidationResult ;
    sh:focusNode ?focus ;
    sh:sourceShape ?shape ;
    sh:sourceConstraintComponent sh:ReifierShapeConstraintComponent ;
    sh:resultPath ?path ;
    sh:resultSeverity sh:Violation ;
    sh:value ?value .
} .`},{file:"skos-entailment.n3",n3:`# Source: rules/skos-entailment.n3
# SKOS Core entailment rules in Notation3 (N3) for Eyeling
# -----------------------------------------------------------------------------
# Implements materialization rules for the normative entailment-relevant parts of
# W3C SKOS Simple Knowledge Organization System Reference sections 3-10.
#
# Scope:
#   * SKOS Core only: http://www.w3.org/2004/02/skos/core#
#   * Covers class/property dependencies from S1-S8, S10-S12, S15-S26,
#     S28-S36, and S38-S45 where they produce positive entailments.
#   * Includes RDF-list traversal only for skos:memberList -> skos:member.
#
# Deliberately out of scope:
#   * SKOS-XL (Appendix B / skosxl: vocabulary).
#   * Integrity constraints and validation checks: S9, S13, S14, S27, S37, S46,
#     SHACL/qSKOS-style quality checks, warnings, and best-practice diagnostics.
#   * Non-entailments called out by the Reference, e.g. skos:broader is not
#     transitive, skos:closeMatch is not transitive, and semantic relations do
#     not imply membership in a shared concept scheme.
#
# Notes:
#   * The executable rules below are intentionally direct SKOS Core rules rather
#     than a general-purpose RDFS/OWL reasoner. This keeps the profile usable on
#     its own and avoids surprising entailments for non-SKOS vocabulary.
#   * S12 states a plain-literal range for SKOS lexical labels. RDF 1.1 systems
#     commonly avoid materializing literal-subject type triples, so this profile
#     records the schema fact but does not emit generalized triples such as
#     "label"@en rdf:type rdf:PlainLiteral.
#   * S32 states that skos:member has a union range of skos:Concept or
#     skos:Collection. The profile records the schema pattern, but does not guess
#     which branch of the union holds for a member value.

@prefix rdf:    <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:   <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl:    <http://www.w3.org/2002/07/owl#> .
@prefix skos:   <http://www.w3.org/2004/02/skos/core#> .
@prefix skosn3: <https://example.org/skos-entailment-n3#> .

#################################################################
# 0. SKOS Core vocabulary facts from sections 3-10
#################################################################

# S1, S2, S28
skos:Concept a owl:Class .
skos:ConceptScheme a owl:Class .
skos:Collection a owl:Class .
skos:OrderedCollection a owl:Class .

# S3, S18, S30, S38
skos:inScheme a owl:ObjectProperty .
skos:hasTopConcept a owl:ObjectProperty .
skos:topConceptOf a owl:ObjectProperty .
skos:semanticRelation a owl:ObjectProperty .
skos:broader a owl:ObjectProperty .
skos:narrower a owl:ObjectProperty .
skos:related a owl:ObjectProperty .
skos:broaderTransitive a owl:ObjectProperty .
skos:narrowerTransitive a owl:ObjectProperty .
skos:member a owl:ObjectProperty .
skos:memberList a owl:ObjectProperty .
skos:mappingRelation a owl:ObjectProperty .
skos:closeMatch a owl:ObjectProperty .
skos:exactMatch a owl:ObjectProperty .
skos:broadMatch a owl:ObjectProperty .
skos:narrowMatch a owl:ObjectProperty .
skos:relatedMatch a owl:ObjectProperty .

# S10, S16
skos:prefLabel a owl:AnnotationProperty .
skos:altLabel a owl:AnnotationProperty .
skos:hiddenLabel a owl:AnnotationProperty .
skos:note a owl:AnnotationProperty .
skos:changeNote a owl:AnnotationProperty .
skos:definition a owl:AnnotationProperty .
skos:editorialNote a owl:AnnotationProperty .
skos:example a owl:AnnotationProperty .
skos:historyNote a owl:AnnotationProperty .
skos:scopeNote a owl:AnnotationProperty .

# S15
skos:notation a owl:DatatypeProperty .

# S4-S8
skos:inScheme rdfs:range skos:ConceptScheme .
skos:hasTopConcept rdfs:domain skos:ConceptScheme ;
  rdfs:range skos:Concept .
skos:topConceptOf rdfs:subPropertyOf skos:inScheme ;
  owl:inverseOf skos:hasTopConcept .

# S11-S12
skos:prefLabel rdfs:subPropertyOf rdfs:label ;
  rdfs:range rdf:PlainLiteral .
skos:altLabel rdfs:subPropertyOf rdfs:label ;
  rdfs:range rdf:PlainLiteral .
skos:hiddenLabel rdfs:subPropertyOf rdfs:label ;
  rdfs:range rdf:PlainLiteral .

# S17
skos:changeNote rdfs:subPropertyOf skos:note .
skos:definition rdfs:subPropertyOf skos:note .
skos:editorialNote rdfs:subPropertyOf skos:note .
skos:example rdfs:subPropertyOf skos:note .
skos:historyNote rdfs:subPropertyOf skos:note .
skos:scopeNote rdfs:subPropertyOf skos:note .

# S19-S26
skos:semanticRelation rdfs:domain skos:Concept ;
  rdfs:range skos:Concept .
skos:broaderTransitive rdfs:subPropertyOf skos:semanticRelation ;
  a owl:TransitiveProperty ;
  owl:inverseOf skos:narrowerTransitive .
skos:narrowerTransitive rdfs:subPropertyOf skos:semanticRelation ;
  a owl:TransitiveProperty .
skos:related rdfs:subPropertyOf skos:semanticRelation ;
  a owl:SymmetricProperty .
skos:broader rdfs:subPropertyOf skos:broaderTransitive ;
  owl:inverseOf skos:narrower .
skos:narrower rdfs:subPropertyOf skos:narrowerTransitive .

# S29-S36
skos:OrderedCollection rdfs:subClassOf skos:Collection .
skos:member rdfs:domain skos:Collection .
skos:memberList rdfs:domain skos:OrderedCollection ;
  rdfs:range rdf:List ;
  a owl:FunctionalProperty .

# S39-S45
skos:mappingRelation rdfs:subPropertyOf skos:semanticRelation .
skos:closeMatch rdfs:subPropertyOf skos:mappingRelation ;
  a owl:SymmetricProperty .
skos:exactMatch rdfs:subPropertyOf skos:closeMatch ;
  a owl:SymmetricProperty, owl:TransitiveProperty .
skos:broadMatch rdfs:subPropertyOf skos:mappingRelation, skos:broader ;
  owl:inverseOf skos:narrowMatch .
skos:narrowMatch rdfs:subPropertyOf skos:mappingRelation, skos:narrower .
skos:relatedMatch rdfs:subPropertyOf skos:mappingRelation, skos:related ;
  a owl:SymmetricProperty .

#################################################################
# 1. Concept schemes (S4-S8)
#################################################################

{ ?resource skos:inScheme ?scheme . }
=> { ?scheme a skos:ConceptScheme . } .

{ ?scheme skos:hasTopConcept ?concept . }
=> { ?scheme a skos:ConceptScheme .
     ?concept a skos:Concept .
     ?concept skos:topConceptOf ?scheme . } .

{ ?concept skos:topConceptOf ?scheme . }
=> { ?concept skos:inScheme ?scheme .
     ?scheme skos:hasTopConcept ?concept . } .

#################################################################
# 2. Lexical labels, notations, and documentation properties
#################################################################

# S11: SKOS lexical label properties are sub-properties of rdfs:label.
{ ?resource skos:prefLabel ?label . }
=> { ?resource rdfs:label ?label . } .

{ ?resource skos:altLabel ?label . }
=> { ?resource rdfs:label ?label . } .

{ ?resource skos:hiddenLabel ?label . }
=> { ?resource rdfs:label ?label . } .

# S17: specific note properties are sub-properties of skos:note.
{ ?resource skos:changeNote ?note . }
=> { ?resource skos:note ?note . } .

{ ?resource skos:definition ?note . }
=> { ?resource skos:note ?note . } .

{ ?resource skos:editorialNote ?note . }
=> { ?resource skos:note ?note . } .

{ ?resource skos:example ?note . }
=> { ?resource skos:note ?note . } .

{ ?resource skos:historyNote ?note . }
=> { ?resource skos:note ?note . } .

{ ?resource skos:scopeNote ?note . }
=> { ?resource skos:note ?note . } .

#################################################################
# 3. Semantic relations (S19-S26)
#################################################################

# S21-S22: property hierarchy.
{ ?concept skos:broader ?broader . }
=> { ?concept skos:broaderTransitive ?broader . } .

{ ?concept skos:narrower ?narrower . }
=> { ?concept skos:narrowerTransitive ?narrower . } .

{ ?subject skos:broaderTransitive ?object . }
=> { ?subject skos:semanticRelation ?object . } .

{ ?subject skos:narrowerTransitive ?object . }
=> { ?subject skos:semanticRelation ?object . } .

{ ?subject skos:related ?object . }
=> { ?subject skos:semanticRelation ?object . } .

# S19-S20: domain/range of skos:semanticRelation.
{ ?subject skos:semanticRelation ?object . }
=> { ?subject a skos:Concept .
     ?object a skos:Concept . } .

# S23: skos:related is symmetric.
{ ?left skos:related ?right . }
=> { ?right skos:related ?left . } .

# S24: transitive broader/narrower closures.
{ ?left skos:broaderTransitive ?middle .
  ?middle skos:broaderTransitive ?right . }
=> { ?left skos:broaderTransitive ?right . } .

{ ?left skos:narrowerTransitive ?middle .
  ?middle skos:narrowerTransitive ?right . }
=> { ?left skos:narrowerTransitive ?right . } .

# S25-S26: inverse hierarchy properties.
{ ?narrower skos:broader ?broader . }
=> { ?broader skos:narrower ?narrower . } .

{ ?broader skos:narrower ?narrower . }
=> { ?narrower skos:broader ?broader . } .

{ ?narrower skos:broaderTransitive ?broader . }
=> { ?broader skos:narrowerTransitive ?narrower . } .

{ ?broader skos:narrowerTransitive ?narrower . }
=> { ?narrower skos:broaderTransitive ?broader . } .

#################################################################
# 4. Concept collections (S29-S36)
#################################################################

# S29: ordered collections are collections.
{ ?collection a skos:OrderedCollection . }
=> { ?collection a skos:Collection . } .

# S31: domain of skos:member.
{ ?collection skos:member ?member . }
=> { ?collection a skos:Collection . } .

# S33-S34: domain/range of skos:memberList.
{ ?collection skos:memberList ?list . }
=> { ?collection a skos:OrderedCollection .
     ?list a rdf:List .
     ?list skosn3:memberListFor ?collection . } .

# S36: every RDF list item in skos:memberList is also a skos:member.
{ ?list skosn3:memberListFor ?collection .
  ?list rdf:first ?member . }
=> { ?collection skos:member ?member . } .

{ ?list skosn3:memberListFor ?collection .
  ?list rdf:rest ?tail .
  ?tail rdf:first ?member . }
=> { ?tail skosn3:memberListFor ?collection . } .

#################################################################
# 5. Mapping properties (S39-S45)
#################################################################

# S39-S42: mapping-property hierarchy.
{ ?left skos:mappingRelation ?right . }
=> { ?left skos:semanticRelation ?right . } .

{ ?left skos:closeMatch ?right . }
=> { ?left skos:mappingRelation ?right . } .

{ ?left skos:exactMatch ?right . }
=> { ?left skos:closeMatch ?right . } .

{ ?left skos:broadMatch ?right . }
=> { ?left skos:mappingRelation ?right .
     ?left skos:broader ?right . } .

{ ?left skos:narrowMatch ?right . }
=> { ?left skos:mappingRelation ?right .
     ?left skos:narrower ?right . } .

{ ?left skos:relatedMatch ?right . }
=> { ?left skos:mappingRelation ?right .
     ?left skos:related ?right . } .

# S43: skos:broadMatch/skos:narrowMatch inverse.
{ ?narrower skos:broadMatch ?broader . }
=> { ?broader skos:narrowMatch ?narrower . } .

{ ?broader skos:narrowMatch ?narrower . }
=> { ?narrower skos:broadMatch ?broader . } .

# S44: relatedMatch, closeMatch, and exactMatch are symmetric.
{ ?left skos:relatedMatch ?right . }
=> { ?right skos:relatedMatch ?left . } .

{ ?left skos:closeMatch ?right . }
=> { ?right skos:closeMatch ?left . } .

{ ?left skos:exactMatch ?right . }
=> { ?right skos:exactMatch ?left . } .

# S45: skos:exactMatch is transitive. Combined with symmetry and S42 this also
# entails the reflexive exactMatch/closeMatch cycle noted in section 10.6.6.
{ ?left skos:exactMatch ?middle .
  ?middle skos:exactMatch ?right . }
=> { ?left skos:exactMatch ?right . } .`}],Oe=C.map(e=>e.n3).join(`

`)});var S,B=j(()=>{S=[{id:"owl-skos-catalog",label:"Catalog topics (OWL 2 RL + SKOS Core)",backgroundFile:"examples/owl-skos-catalog/ontology.n3",dataFile:"examples/owl-skos-catalog/input.trig",background:`# Combined OWL 2 RL + SKOS Core example background.
#
# OWL/RDFS rules are needed to lift :DatasetConcept instances to skos:Concept
# and to map :relatedTopic to skos:related. SKOS rules are then needed for
# skos:related symmetry, skos:semanticRelation, and SKOS domain/range typing.

@prefix : <https://example.org/catalog#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix skos: <http://www.w3.org/2004/02/skos/core#> .

:DatasetConcept rdfs:subClassOf skos:Concept .
:relatedTopic owl:equivalentProperty skos:related .
`,data:`# RDF input for the combined OWL 2 RL + SKOS Core catalog example.

PREFIX : <https://example.org/catalog#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

:openData a :DatasetConcept ;
  :relatedTopic :transportData .
`},{id:"complex-path-coverage",label:"Coverage audit (OWL 2 RL + SKOS + SHACL paths)",backgroundFile:"examples/complex-path-coverage/ontology.n3",dataFile:"examples/complex-path-coverage/input.trig",background:`# Background ontology, SKOS taxonomy, and SHACL shape for the complex-path coverage example.

@prefix :     <https://example.org/coverage#> .
@prefix owl:  <http://www.w3.org/2002/07/owl#> .
@prefix rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix sh:   <http://www.w3.org/ns/shacl#> .
@prefix skos: <http://www.w3.org/2004/02/skos/core#> .

:Asset a owl:Class .
:CatalogRecord a owl:Class .
:ReviewedCatalogRecord a owl:Class .
:QualityCheckedRecord rdfs:subClassOf :ReviewedCatalogRecord .

:describes a owl:ObjectProperty ;
  rdfs:domain :CatalogRecord ;
  rdfs:range :Asset .

:theme a owl:ObjectProperty ;
  rdfs:domain :Asset ;
  rdfs:range skos:Concept ;
  owl:propertyChainAxiom ( :theme skos:broaderTransitive ) .

:scheme a skos:ConceptScheme .

:vehicle a skos:Concept, :RequiredCoverageConcept ;
  skos:prefLabel "Vehicles" ;
  skos:topConceptOf :scheme .

:bus a skos:Concept ;
  skos:prefLabel "Buses" ;
  skos:broader :vehicle ;
  skos:inScheme :scheme .

:electricBus a skos:Concept ;
  skos:prefLabel "Electric buses" ;
  skos:broader :bus ;
  skos:inScheme :scheme .

:dieselBus a skos:Concept ;
  skos:prefLabel "Diesel buses" ;
  skos:broader :bus ;
  skos:inScheme :scheme .

# From the required broad concept, walk backwards over inferred asset themes and
# then backwards again to the catalog records that describe those assets:
#   :vehicle <- :theme - ?asset <- :describes - ?record
:VehicleReviewShape a sh:NodeShape ;
  sh:targetNode :vehicle ;
  sh:path ( [ sh:inversePath :theme ] [ sh:inversePath :describes ] ) ;
  sh:class :ReviewedCatalogRecord .
`,data:`# Catalog records and assets for the complex-path coverage example.

PREFIX : <https://example.org/coverage#>

:electric-bus-card a :QualityCheckedRecord ;
  :describes :bus-42 .

:bus-42 a :Asset ;
  :theme :electricBus .

:diesel-bus-draft a :CatalogRecord ;
  :describes :bus-77 .

:bus-77 a :Asset ;
  :theme :dieselBus .
`},{id:"shacl12-grandfather",label:"Grandfather classification (SHACL 1.2 + OWL RL)",backgroundFile:"examples/shacl12-grandfather/shapes.n3",dataFile:"examples/shacl12-grandfather/input.trig",background:`# SHACL 1.2 targetWhere example based on Holger Knublauch's grandfather shape, but with OWL inferencing example
# Source: https://www.linkedin.com/pulse/inferencing-support-owl-shacl-12-classification-holger-knublauch-bhjjc/

@prefix ex: <https://example.org/shacl12-grandfather#> .
@prefix sh: <http://www.w3.org/ns/shacl#> .
@prefix :    <https://example.org/family#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:<http://www.w3.org/2000/01/rdf-schema#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#>.

ex:Grandfather
  a owl:Class ;
  rdfs:subClassOf ex:Person ;
  owl:equivalentClass [
    a owl:Class ;
    owl:intersectionOf (
      [
        a owl:Restriction ;
        owl:onProperty ex:gender ;
        owl:hasValue "male"
      ]
      [
        a owl:Restriction ;
        owl:onProperty ex:hasGrandchild ;
        owl:someValuesFrom owl:Thing
      ]
    )
  ] .

ex:hasGrandchild
  a owl:ObjectProperty ;
  owl:propertyChainAxiom ( ex:child ex:child ) .

## Both shapes are enabled by default:
## - GrandfatherShape1 demonstrates the SHACL 1.2 sh:targetWhere pattern.
## - GrandfatherShape2 demonstrates the simpler OWL RL symbiosis path, where
##   sh:targetClass can use the inferred ex:Grandfather classification.

ex:GrandfatherShape2 sh:targetClass ex:Grandfather ; sh:property [ sh:path ex:name ; sh:minCount 1 ] .

ex:GrandfatherShape1
  a sh:NodeShape ;
  sh:targetWhere [
    sh:property [
      sh:path ex:gender ;
      sh:hasValue "male" ;
    ] ;
    sh:property [
      sh:path ( ex:child ex:child ) ;
      sh:minCount 1 ;
    ] ;
  ] ;
  # This constraint makes the targetWhere result visible in the demo:
  # Grandfathers must have a name
  sh:property [ sh:path ex:name ; sh:minCount 1 ]  .
`,data:`# RDF input for the SHACL 1.2 + OWL RL grandfather example.

PREFIX ex: <https://example.org/shacl12-grandfather#>

ex:P1 a ex:Person ;
  ex:child ex:P1.1, ex:P1.2 ;
  ex:gender "male" .

ex:P1.1 a ex:Person ;
  ex:child ex:P1.1.1 .

ex:P1.1.1 a ex:Person .

ex:P1.2 a ex:Person ;
  ex:child ex:P1.2.1, ex:P1.2.2 .

ex:P1.2.1 a ex:Person .
ex:P1.2.2 a ex:Person .
`},{id:"inconsistency-diagnostics",label:"Inconsistency diagnostics (OWL 2 RL)",backgroundFile:"examples/inconsistency-diagnostics/ontology.n3",dataFile:"examples/inconsistency-diagnostics/input.trig",background:`# Background ontology for the inconsistency diagnostics playground example.

@prefix : <https://example.org/test#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .

:A owl:disjointWith :B .
`,data:`# RDF input for the inconsistency diagnostics playground example.

PREFIX : <https://example.org/test#>

:x a :A .
:x a :B .`},{id:"shacl-shape-planning",label:"SHACL shape planning (SHACL in/out hints)",backgroundFile:"examples/shacl-shape-planning/ontology.n3",dataFile:"examples/shacl-shape-planning/input.messages.trig",background:`@prefix ex:   <https://example.org/shape-planning#> .
@prefix sosa: <http://www.w3.org/ns/sosa/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .

sosa:madeBySensor
  rdfs:domain sosa:Observation ;
  rdfs:range sosa:Sensor .

sosa:resultTime
  rdfs:domain sosa:Observation .

sosa:hasSimpleResult
  rdfs:domain sosa:Observation .

sosa:hasFeatureOfInterest
  rdfs:domain sosa:Observation ;
  rdfs:range sosa:FeatureOfInterest .

ex:observedBy
  rdfs:subPropertyOf sosa:madeBySensor .

ex:observedAt
  rdfs:subPropertyOf sosa:resultTime .

ex:temperatureCelsius
  rdfs:subPropertyOf sosa:hasSimpleResult .

ex:observedFeature
  rdfs:subPropertyOf sosa:hasFeatureOfInterest .
`,data:`# RDF Messages input for the SHACL shape planning example.

VERSION "1.2-messages"
PREFIX ex:   <https://example.org/shape-planning#>
PREFIX sosa: <http://www.w3.org/ns/sosa/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>

ex:obs1 ex:observedBy ex:sensor1 ;
	ex:observedAt "2026-06-16T10:00:01Z"^^xsd:dateTime ;
	ex:temperatureCelsius "18.1"^^xsd:decimal ;
	ex:observedFeature ex:platformA .
ex:obs1 ex:debugOnly "drop me 1" .

MESSAGE
ex:obs2 ex:observedBy ex:sensor2 ;
	ex:observedAt "2026-06-16T10:00:02Z"^^xsd:dateTime ;
	ex:temperatureCelsius "18.3"^^xsd:decimal ;
	ex:observedFeature ex:platformA .
ex:obs2 ex:debugOnly "drop me 2" .

MESSAGE
ex:obs3 ex:observedBy ex:sensor3 ;
	ex:observedAt "2026-06-16T10:00:03Z"^^xsd:dateTime ;
	ex:temperatureCelsius "18.6"^^xsd:decimal ;
	ex:observedFeature ex:platformB .
ex:obs3 ex:debugOnly "drop me 3" .

MESSAGE
ex:obs4 ex:observedBy ex:sensor4 ;
	ex:observedAt "2026-06-16T10:00:04Z"^^xsd:dateTime ;
	ex:temperatureCelsius "18.8"^^xsd:decimal ;
	ex:observedFeature ex:platformB .
ex:obs4 ex:debugOnly "drop me 4" .

MESSAGE
ex:obs5 ex:observedBy ex:sensor5 ;
	ex:observedAt "2026-06-16T10:00:05Z"^^xsd:dateTime ;
	ex:temperatureCelsius "19.0"^^xsd:decimal ;
	ex:observedFeature ex:platformC .
ex:obs5 ex:debugOnly "drop me 5" .

MESSAGE
ex:obs6 ex:observedBy ex:sensor1 ;
	ex:observedAt "2026-06-16T10:00:06Z"^^xsd:dateTime ;
	ex:temperatureCelsius "19.2"^^xsd:decimal ;
	ex:observedFeature ex:platformC .
ex:obs6 ex:debugOnly "drop me 6" .

MESSAGE
ex:obs7 ex:observedBy ex:sensor2 ;
	ex:observedAt "2026-06-16T10:00:07Z"^^xsd:dateTime ;
	ex:temperatureCelsius "19.5"^^xsd:decimal ;
	ex:observedFeature ex:platformD .
ex:obs7 ex:debugOnly "drop me 7" .

MESSAGE
ex:obs8 ex:observedBy ex:sensor3 ;
	ex:observedAt "2026-06-16T10:00:08Z"^^xsd:dateTime ;
	ex:temperatureCelsius "19.7"^^xsd:decimal ;
	ex:observedFeature ex:platformD .
ex:obs8 ex:debugOnly "drop me 8" .

MESSAGE
ex:obs9 ex:observedBy ex:sensor4 ;
	ex:observedAt "2026-06-16T10:00:09Z"^^xsd:dateTime ;
	ex:temperatureCelsius "19.9"^^xsd:decimal ;
	ex:observedFeature ex:platformE .
ex:obs9 ex:debugOnly "drop me 9" .

MESSAGE
ex:obs10 ex:observedBy ex:sensor5 ;
	ex:observedAt "2026-06-16T10:00:10Z"^^xsd:dateTime ;
	ex:temperatureCelsius "20.0"^^xsd:decimal ;
	ex:observedFeature ex:platformE .
ex:obs10 ex:debugOnly "drop me 10" .

MESSAGE
ex:obs11 ex:observedBy ex:sensor1 ;
	ex:observedAt "2026-06-16T10:00:11Z"^^xsd:dateTime ;
	ex:temperatureCelsius "20.2"^^xsd:decimal ;
	ex:observedFeature ex:platformA .
ex:obs11 ex:debugOnly "drop me 11" .

MESSAGE
ex:obs12 ex:observedBy ex:sensor2 ;
	ex:observedAt "2026-06-16T10:00:12Z"^^xsd:dateTime ;
	ex:temperatureCelsius "20.4"^^xsd:decimal ;
	ex:observedFeature ex:platformB .
ex:obs12 ex:debugOnly "drop me 12" .

MESSAGE
ex:obs13 ex:observedBy ex:sensor3 ;
	ex:observedAt "2026-06-16T10:00:13Z"^^xsd:dateTime ;
	ex:temperatureCelsius "20.6"^^xsd:decimal ;
	ex:observedFeature ex:platformC .
ex:obs13 ex:debugOnly "drop me 13" .

MESSAGE
ex:obs14 ex:observedBy ex:sensor4 ;
	ex:observedAt "2026-06-16T10:00:14Z"^^xsd:dateTime ;
	ex:temperatureCelsius "20.8"^^xsd:decimal ;
	ex:observedFeature ex:platformD .
ex:obs14 ex:debugOnly "drop me 14" .

MESSAGE
ex:obs15 ex:observedBy ex:sensor5 ;
	ex:observedAt "2026-06-16T10:00:15Z"^^xsd:dateTime ;
	ex:temperatureCelsius "21.0"^^xsd:decimal ;
	ex:observedFeature ex:platformE .
ex:obs15 ex:debugOnly "drop me 15" .

MESSAGE
ex:obs16 ex:observedBy ex:sensor1 ;
	ex:observedAt "2026-06-16T10:00:16Z"^^xsd:dateTime ;
	ex:temperatureCelsius "21.1"^^xsd:decimal ;
	ex:observedFeature ex:platformA .
ex:obs16 ex:debugOnly "drop me 16" .

MESSAGE
ex:obs17 ex:observedBy ex:sensor2 ;
	ex:observedAt "2026-06-16T10:00:17Z"^^xsd:dateTime ;
	ex:temperatureCelsius "21.3"^^xsd:decimal ;
	ex:observedFeature ex:platformB .
ex:obs17 ex:debugOnly "drop me 17" .

MESSAGE
ex:obs18 ex:observedBy ex:sensor3 ;
	ex:observedAt "2026-06-16T10:00:18Z"^^xsd:dateTime ;
	ex:temperatureCelsius "21.5"^^xsd:decimal ;
	ex:observedFeature ex:platformC .
ex:obs18 ex:debugOnly "drop me 18" .

MESSAGE
ex:obs19 ex:observedBy ex:sensor4 ;
	ex:observedAt "2026-06-16T10:00:19Z"^^xsd:dateTime ;
	ex:temperatureCelsius "21.7"^^xsd:decimal ;
	ex:observedFeature ex:platformD .
ex:obs19 ex:debugOnly "drop me 19" .

MESSAGE
ex:obs20 ex:observedBy ex:sensor5 ;
	ex:observedAt "2026-06-16T10:00:20Z"^^xsd:dateTime ;
	ex:temperatureCelsius "21.9"^^xsd:decimal ;
	ex:observedFeature ex:platformE .
ex:obs20 ex:debugOnly "drop me 20" .
`,shaclInFile:"examples/shacl-shape-planning/shapes-in.n3",shaclOutFile:"examples/shacl-shape-planning/shapes-out.n3",shaclIn:`@prefix ex:   <https://example.org/shape-planning#> .
@prefix sh:   <http://www.w3.org/ns/shacl#> .
@prefix sosa: <http://www.w3.org/ns/sosa/> .
@prefix xsd:  <http://www.w3.org/2001/XMLSchema#> .

ex:ObservationInputShape
  a sh:NodeShape ;
  sh:closed true ;
  sh:targetClass sosa:ObservationMessage ;
  sh:property [
    sh:path ex:observedBy ;
    sh:minCount 1 ;
    sh:maxCount 1 ;
    sh:nodeKind sh:IRI
  ] ;
  sh:property [
    sh:path ex:observedAt ;
    sh:minCount 1 ;
    sh:maxCount 1 ;
    sh:datatype xsd:dateTime
  ] ;
  sh:property [
    sh:path ex:temperatureCelsius ;
    sh:minCount 1 ;
    sh:maxCount 1 ;
    sh:datatype xsd:decimal
  ] ;
  sh:property [
    sh:path ex:observedFeature ;
    sh:minCount 1 ;
    sh:maxCount 1 ;
    sh:nodeKind sh:IRI
  ] .
`,shaclOut:`@prefix ex:   <https://example.org/shape-planning#> .
@prefix rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix sh:   <http://www.w3.org/ns/shacl#> .
@prefix sosa: <http://www.w3.org/ns/sosa/> .
@prefix xsd:  <http://www.w3.org/2001/XMLSchema#> .

ex:ObservationOutputShape
  a sh:NodeShape ;
  sh:targetSubjectsOf sosa:madeBySensor ;
  sh:property [
    sh:path sosa:madeBySensor ;
    sh:maxCount 1
  ] ;
  sh:property [
    sh:path sosa:resultTime ;
    sh:maxCount 1 ;
    sh:datatype xsd:dateTime
  ] ;
  sh:property [
    sh:path sosa:hasSimpleResult ;
    sh:maxCount 1 ;
    sh:datatype xsd:decimal
  ] ;
  sh:property [
    sh:path sosa:hasFeatureOfInterest ;
    sh:maxCount 1 ;
    sh:nodeKind sh:IRI
  ] ;
  sh:property [
    sh:path rdf:type ;
    sh:hasValue sosa:Observation
  ] .

ex:SensorOutputShape
  a sh:NodeShape ;
  sh:targetObjectsOf sosa:madeBySensor ;
  sh:property [
    sh:path rdf:type ;
    sh:hasValue sosa:Sensor
  ] .

ex:FeatureOutputShape
  a sh:NodeShape ;
  sh:targetObjectsOf sosa:hasFeatureOfInterest ;
  sh:property [
    sh:path rdf:type ;
    sh:hasValue sosa:FeatureOfInterest
  ] .
`},{id:"shacl-validation",label:"SHACL validation (SHACL Core)",backgroundFile:"examples/shacl-validation/shapes.n3",dataFile:"examples/shacl-validation/input.trig",background:`# SHACL shapes for the validation example.

@prefix ex:   <https://example.org/shacl#> .
@prefix sh:   <http://www.w3.org/ns/shacl#> .
@prefix xsd:  <http://www.w3.org/2001/XMLSchema#> .

ex:PersonShape
  a sh:NodeShape ;
  sh:targetClass ex:Person ;
  sh:property ex:PersonShape-name ;
  sh:property ex:PersonShape-age ;
  sh:property ex:PersonShape-email .

ex:PersonShape-name
  a sh:PropertyShape ;
  sh:path ex:name ;
  sh:minCount 1 ;
  sh:datatype xsd:string .

ex:PersonShape-age
  a sh:PropertyShape ;
  sh:path ex:age ;
  sh:minInclusive 0 ;
  sh:maxInclusive 120 .

ex:PersonShape-email
  a sh:PropertyShape ;
  sh:path ex:email ;
  sh:maxCount 1 ;
  sh:pattern "^[^@]+@[^@]+$" .
`,data:`# RDF data for the SHACL validation example.

PREFIX ex: <https://example.org/shacl#>

ex:Alice a ex:Person ;
  ex:name "Alice" ;
  ex:age 42 ;
  ex:email "alice@example.org" .

ex:Bob a ex:Person ;
  ex:age -1 ;
  ex:email "not-an-email", "bob@example.org" .
`},{id:"shipment-logistics",label:"Shipment logistics (OWL 2 RL)",backgroundFile:"examples/shipment-logistics/ontology.n3",dataFile:"examples/shipment-logistics/input.trig",background:`# More elaborate OWL 2 RL demo vocabulary.

@prefix :    <https://example.org/logistics#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:<http://www.w3.org/2000/01/rdf-schema#> .

:Shipment a owl:Class .
:ExpressShipment a owl:Class ;
  owl:equivalentClass :PriorityShipment ;
  rdfs:subClassOf :Shipment .
:FragileShipment a owl:Class ;
  rdfs:subClassOf [
    a owl:Restriction ;
    owl:onProperty :requiresHandling ;
    owl:hasValue :carefulHandling
  ] .
:CountryDestinationShipment a owl:Class ;
  owl:equivalentClass [
    a owl:Restriction ;
    owl:onProperty :destinationCountry ;
    owl:someValuesFrom :Country
  ] .
:RegulatedShipment a owl:Class ;
  owl:equivalentClass [
    a owl:Restriction ;
    owl:onProperty :contains ;
    owl:allValuesFrom :RegulatedItem
  ] .
:Country a owl:Class .
:RegulatedItem a owl:Class .

:contains a owl:ObjectProperty ;
  owl:equivalentProperty :hasPart .
:handledBy a owl:ObjectProperty ;
  owl:inverseOf :handles .
:partnerOf a owl:ObjectProperty, owl:SymmetricProperty .
:locatedIn a owl:ObjectProperty, owl:TransitiveProperty .
:trackingCode a owl:ObjectProperty, owl:FunctionalProperty .
:manifestId a owl:ObjectProperty, owl:InverseFunctionalProperty .

:destinationCountry a owl:ObjectProperty .
:requiresHandling a owl:ObjectProperty .
`,data:`# RDF input for the shipment logistics OWL 2 RL example.

PREFIX : <https://example.org/logistics#>

:shipment-1 a :PriorityShipment, :FragileShipment ;
  :contains :box-1 ;
  :destinationCountry :belgium ;
  :handledBy :alice ;
  :trackingCode :tracking-a, :tracking-b .

:belgium a :Country .
:alice :partnerOf :bob .
:box-1 :locatedIn :pallet-1 .
:pallet-1 :locatedIn :warehouse-1 .

:shipment-2 :manifestId :manifest-7 .
:shipment-3 :manifestId :manifest-7 .

:shipment-4 a :RegulatedShipment ;
  :contains :item-9 .
`},{id:"skos-taxonomy",label:"SKOS taxonomy (SKOS Core)",backgroundFile:"examples/skos-taxonomy/ontology.n3",dataFile:"examples/skos-taxonomy/input.trig",background:`# SKOS Core example background.

@prefix : <https://example.org/subjects#> .
@prefix skos: <http://www.w3.org/2004/02/skos/core#> .

:scheme a skos:ConceptScheme .
`,data:`# RDF input for the SKOS Core taxonomy example.

PREFIX : <https://example.org/subjects#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

:cat skos:prefLabel "Cat" ;
  skos:definition "Small domesticated carnivorous mammal." ;
  skos:broader :mammal .

:mammal skos:broader :animal .
:animal skos:topConceptOf :scheme .
`},{id:"stateful-materialization",label:"Stateful materialization (RDF Messages)",backgroundFile:"examples/stateful-materialization/ontology.n3",dataFile:"examples/stateful-materialization/input.messages.trig",background:`# Background ontology for the stateful materialization example.

@prefix :    <https://example.org/family#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:<http://www.w3.org/2000/01/rdf-schema#> .

:Female a owl:Class .
:Parent a owl:Class .
:Mother a owl:Class ;
## Added to show more OWL functionality here
  owl:equivalentClass :FemaleParent .

:FemaleParent owl:intersectionOf ( :Female :Parent ) .

:hasParent rdfs:range :Parent .
`,data:`# RDF Messages input for the stateful materialization example.
# The two facts are split across messages on purpose.

VERSION "1.2-messages"
PREFIX : <https://example.org/family#>

:alice a :Female .

MESSAGE
:bob :hasParent :alice .
`},{id:"transit-fleet",label:"Transit fleet (OWL/RDFS)",backgroundFile:"examples/transit-fleet/ontology.n3",dataFile:"examples/transit-fleet/input.trig",background:`# Small demo vocabulary for RDFS ingest enrichment with Eyeling.

@prefix :    <https://example.org/transit#> .
@prefix rdfs:<http://www.w3.org/2000/01/rdf-schema#> .

:Bus rdfs:subClassOf :Vehicle .
:ElectricBus rdfs:subClassOf :Bus .

:operatedBy rdfs:domain :Vehicle ;
  rdfs:range :Operator .
`,data:`# RDF input for the example.
#
# Run with:
#   npm run example:transit-fleet

PREFIX : <https://example.org/transit#>

# Input facts. The generated runtime rules infer:
#   :bus-42 a :Bus, :Vehicle
#   :operator-7 a :Operator
:bus-42 a :ElectricBus ;
  :operatedBy :operator-7 .
`},{id:"transit-messages",label:"Transit stream (RDF Messages)",backgroundFile:"examples/transit-messages/ontology.n3",dataFile:"examples/transit-messages/input.messages.trig",background:`# Background vocabulary for the RDF Messages transit example.

@prefix :    <https://example.org/transit#> .
@prefix rdfs:<http://www.w3.org/2000/01/rdf-schema#> .

:Bus rdfs:subClassOf :Vehicle .
:ElectricBus rdfs:subClassOf :Bus .

:operatedBy rdfs:domain :Vehicle ;
  rdfs:range :Operator .
`,data:`# RDF Messages input for the transit example.

VERSION "1.2-messages"
PREFIX : <https://example.org/transit#>

:bus-42 a :ElectricBus .

MESSAGE
:bus-43 :operatedBy :operator-8 .
`}]});var Re=te(()=>{W();B();var u={backgroundMode:"text",dataMode:"text",backgroundText:f().background,dataText:f().data},r={backgroundText:x("backgroundText",u.backgroundText),dataText:x("dataText",u.dataText),shaclInText:x("shaclInText",""),shaclOutText:x("shaclOutText",""),outputText:x("outputText","")},t={exampleSelect:A("exampleSelect"),backgroundMode:A("backgroundMode"),dataMode:A("dataMode"),statefulMaterialization:M("statefulMaterialization"),backgroundUrl:M("backgroundUrl"),dataUrl:M("dataUrl"),backgroundUrlPanel:c("backgroundUrlPanel"),backgroundTextPanel:c("backgroundTextPanel"),dataUrlPanel:c("dataUrlPanel"),dataTextPanel:c("dataTextPanel"),shapeHintsPanel:c("shapeHintsPanel"),runButton:k("runButton"),stopButton:k("stopButton"),resetButton:k("resetButton"),shareButton:k("shareButton"),status:c("status"),runtimeStats:c("runtimeStats"),rulesSummary:G("rulesSummary"),ruleProfileList:G("ruleProfileList")},h=!1,z=0,w=null,b="",m=0;we();ye();Ce();O();se();y();F(!1);g("Ready. Choose an example, URL, or text input, then run inference.");function x(e,n){let s=document.getElementById(e);if(!s)throw new Error(`Missing textarea #${e}`);return s.value=n,CodeMirror.fromTextArea(s,{mode:"text/turtle",lineNumbers:!0,lineWrapping:!0,tabSize:2,viewportMargin:1/0})}function se(){t.runButton.addEventListener("click",()=>{re()}),t.stopButton.addEventListener("click",pe),t.resetButton.addEventListener("click",ge),t.exampleSelect.addEventListener("change",()=>be(t.exampleSelect.value)),t.shareButton.addEventListener("click",()=>{P(),navigator.clipboard?.writeText(window.location.href),g("Shareable URL copied when clipboard access is available.")});for(let e of[t.backgroundMode,t.dataMode])e.addEventListener("change",()=>{O(),y()});for(let e of Object.values(r))e.on("change",y);t.shapeHintsPanel.addEventListener("toggle",()=>{window.setTimeout(()=>{r.shaclInText.refresh(),r.shaclOutText.refresh()},0)});for(let e of[t.backgroundUrl,t.dataUrl])e.addEventListener("input",y);t.statefulMaterialization.addEventListener("change",y)}async function re(){if(w)return;let e={controller:new AbortController,startedAt:performance.now()};w=e;try{F(!0),t.runtimeStats.textContent="",t.runtimeStats.hidden=!0,_(),de(e),r.outputText.setValue(""),Y(e,"Preparing inference\u2026");let n=await ie("background",e.controller.signal);me(e.controller.signal);let s=d("data"),a=s==="text"?r.dataText.getValue():void 0,o=s==="url"?t.dataUrl.value.trim():void 0,i=r.shaclInText.getValue().trim()||void 0,p=r.shaclOutText.getValue().trim()||void 0;if(s==="url"&&!o)throw new Error("Enter a data URL or switch to text input.");let l=Z();if(l.length===0)throw new Error("Select at least one bundled N3 rule file in the advanced rule profile selection.");await ae(e,{apiScriptUrl:new URL("browser/rdfjs-inference-engine.min.js",window.location.href).href,bundledRules:l.map(L=>L.n3).join(`

`),bundledRuleProfiles:l,bundledRuleCount:l.length,bundledRuleLabels:l.map(L=>L.file),backgroundSource:n,dataMode:s,statefulMaterialization:t.statefulMaterialization.checked,statefulStoreName:t.statefulMaterialization.checked?oe(n,s==="url"?o??"":a??""):void 0,shaclInSource:i,shaclOutSource:p,dataSource:a,dataUrl:o})}catch(n){if(fe(n))q(),e.statusMessage="Stopped. No more messages or quads will be processed.";else{let s=n instanceof Error?n.message:String(n);_(),e.statusMessage=`Error: ${s}`,r.outputText.setValue(s)}D(e)}finally{D(e),w===e&&(w=null),e.worker?.terminate(),F(!1)}}function ae(e,n){return new Promise((s,a)=>{let o=le();e.worker=o;let i=()=>{o.terminate(),a(new DOMException("Inference was stopped.","AbortError"))};e.controller.signal.addEventListener("abort",i,{once:!0}),o.onmessage=p=>{let l=p.data;l.type==="status"?Y(e,l.message):l.type==="runtime"?(e.runtimeMessage=l.message,I(e)):l.type==="append"?ce(l.chunk):l.type==="result"?(e.statusMessage=l.status,l.output!==void 0?(_(),r.outputText.setValue(l.output)):q(),D(e),s()):l.type==="error"&&a(new Error(l.message))},o.onerror=p=>{a(new Error(p.message))},o.postMessage(n)})}function oe(e,n){let s=[window.location.origin,window.location.pathname,e,n].join("\0");return`rdfjs-inference-engine:playground:${X(s)}`}function X(e){let n=3735928559,s=1103547991;for(let a=0;a<e.length;a+=1){let o=e.charCodeAt(a);n=Math.imul(n^o,2654435761),s=Math.imul(s^o,1597334677)}return n=Math.imul(n^n>>>16,2246822507)^Math.imul(s^s>>>13,3266489909),s=Math.imul(s^s>>>16,2246822507)^Math.imul(n^n>>>13,3266489909),`${(s>>>0).toString(36)}${(n>>>0).toString(36)}`}function le(){let e=`
self.onmessage = async (event) => {
  const request = event.data;
  try {
    self.currentWorkerRequest = request;
    importScripts(request.apiScriptUrl);
    const api = self.RdfjsInferenceEngine;
    if (!api) {
      throw new Error('Could not load the browser inference engine bundle.');
    }

    self.postMessage({ type: 'status', message: 'Parsing background knowledge\u2026' });
    const background = api.parseRdfOrMessages(request.backgroundSource);
    self.postMessage({ type: 'status', message: 'Parsed ' + background.quads.length + ' background quad(s). Compiling runtime\u2026' });

    const reasoner = new api.InferenceEngine();
    const started = performance.now();
    const loadOptions = {};
    if (request.statefulMaterialization) {
      loadOptions.skolemKey = request.statefulStoreName || 'rdfjs-inference-engine:playground:default';
    }
    if (request.shaclInSource) {
      loadOptions.shaclIn = request.shaclInSource;
    }
    if (request.shaclOutSource) {
      loadOptions.shaclOut = request.shaclOutSource;
    }
    const ruleLabel = request.bundledRuleLabels && request.bundledRuleLabels.length
      ? 'Bundled profiles: ' + request.bundledRuleLabels.join(', ')
      : 'Bundled N3 rule profiles';
    const ruleProfiles = request.bundledRuleProfiles && request.bundledRuleProfiles.length
      ? request.bundledRuleProfiles.map((profile) => ({ n3: profile.n3, label: profile.file }))
      : [{ n3: request.bundledRules, label: ruleLabel }];
    const runtime = reasoner.load(ruleProfiles, background.quads, Object.keys(loadOptions).length ? loadOptions : undefined);
    const compiledAt = performance.now();
    const shapeHintSummary = (request.shaclInSource || request.shaclOutSource) ? ' \xB7 SHACL hints ' + [request.shaclInSource ? 'in' : '', request.shaclOutSource ? 'out' : ''].filter(Boolean).join('/') : '';
    self.postMessage({ type: 'runtime', message: 'Background ' + countLabel(background.quads.length, 'quad') + ' \xB7 Rule profiles ' + request.bundledRuleCount + shapeHintSummary + ' \xB7 Runtime ' + (runtime.length / 1024).toFixed(1) + ' KiB' });

    await processInputData(api, reasoner, request, compiledAt, started);
  } catch (error) {
    self.postMessage({ type: 'error', message: error instanceof Error ? error.message : String(error) });
  } finally {
    self.currentWorkerRequest = null;
  }
};

async function processInputData(api, reasoner, request, compiledAt, started) {
  if (request.dataMode === 'url') {
    await processUrlInput(api, reasoner, request.dataUrl, compiledAt, started);
    return;
  }

  await processTextInput(api, reasoner, request.dataSource || '', compiledAt, started);
}

async function processTextInput(api, reasoner, source, compiledAt, started) {
  self.postMessage({ type: 'status', message: 'Parsing text input\u2026' });
  const state = createStreamingState(api, reasoner, compiledAt, started, 'text input', requestStatefulMaterialization(), requestStatefulStoreName());
  await handleParsedItems(state, state.parser.write(source));
  await handleParsedItems(state, state.parser.end());
  await finishStreamingState(state);
}

async function processUrlInput(api, reasoner, url, compiledAt, started) {
  if (!url) {
    throw new Error('Missing data URL.');
  }

  self.postMessage({ type: 'status', message: 'Fetching input data stream\u2026' });
  const response = await fetch(url);
  if (!response.ok) {
    throw new Error('Could not fetch ' + url + ': ' + response.status + ' ' + response.statusText);
  }
  if (!response.body) {
    const source = await response.text();
    await processTextInput(api, reasoner, source, compiledAt, started);
    return;
  }

  const state = createStreamingState(api, reasoner, compiledAt, started, url, requestStatefulMaterialization(), requestStatefulStoreName());
  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  let bytes = 0;

  for (;;) {
    const read = await reader.read();
    if (read.done) {
      break;
    }
    bytes += read.value.byteLength;
    const text = decoder.decode(read.value, { stream: true });
    await handleParsedItems(state, state.parser.write(text));
    postProgressStatus(state, progressMessage(state, 'Streaming input: read ' + Math.round(bytes / 1024) + ' KiB'));
  }

  const tail = decoder.decode();
  if (tail) {
    await handleParsedItems(state, state.parser.write(tail));
  }
  await handleParsedItems(state, state.parser.end());
  await finishStreamingState(state);
}

function requestStatefulMaterialization() {
  return Boolean(self.currentWorkerRequest && self.currentWorkerRequest.statefulMaterialization);
}

function requestStatefulStoreName() {
  return self.currentWorkerRequest && self.currentWorkerRequest.statefulStoreName
    ? String(self.currentWorkerRequest.statefulStoreName)
    : 'rdfjs-inference-engine:playground:default';
}

function createStreamingState(api, reasoner, compiledAt, started, sourceLabel, statefulMaterialization, statefulStoreName) {
  return {
    api,
    reasoner,
    parser: new api.IncrementalParser({ factory: api.DataFactory }),
    sourceLabel,
    compiledAt,
    started,
    statefulMaterialization,
    statefulStoreName,
    messagesMode: false,
    ordinaryQuads: [],
    currentMessage: [],
    currentMessageCounter: 0,
    parsedQuadCount: 0,
    processedMessageCount: 0,
    inferredCount: 0,
    inconsistencyComments: [],
    lastStatusAt: 0,
    writer: null,
  };
}

async function handleParsedItems(state, items) {
  for (const item of items) {
    if (state.api.isMessageQuad(item)) {
      state.messagesMode = true;
      while (state.currentMessageCounter < item.messageCounter) {
        await processCurrentMessage(state);
        state.currentMessageCounter += 1;
        state.currentMessage = [];
      }
      state.currentMessage.push(item.quad);
      state.parsedQuadCount += 1;
    } else if (state.messagesMode) {
      throw new Error('Cannot mix RDF Messages and ordinary RDF parser output in one input stream.');
    } else {
      state.ordinaryQuads.push(item);
      state.parsedQuadCount += 1;
    }
  }
}

async function finishStreamingState(state) {
  if (state.messagesMode) {
    await processCurrentMessage(state);
    if (state.writer) {
      await endWriter(state.writer);
    }
    appendInconsistencyComments(state.inconsistencyComments);
    self.postMessage({ type: 'result', status: 'Done \xB7 RDF Messages: ' + state.processedMessageCount + ' message(s), ' + state.parsedQuadCount + ' quad(s), ' + state.inferredCount + ' inferred' + diagnosticStatusSuffix(state.inconsistencyComments) + statefulStoreSummary(state) });
    return;
  }

  const total = state.ordinaryQuads.length;
  self.postMessage({ type: 'status', message: 'Parsed ' + total + ' input quad(s). Running inference\u2026' });
  const inference = state.reasoner.inferWithDiagnostics(state.ordinaryQuads);
  const comments = formatInconsistencyComments(inference.inconsistencies);
  self.postMessage({ type: 'status', message: 'Processed ' + total + ' input quad(s). Serializing ' + inference.quads.length + ' inferred quad(s)\u2026' });
  const output = await state.api.writeQuads(inference.quads, outputPrefixes());
  self.postMessage({ type: 'result', output: comments + output, status: 'Done \xB7 RDF input: ' + total + ' quad(s), ' + inference.quads.length + ' inferred' + diagnosticStatusSuffix(comments) });
}

async function processCurrentMessage(state) {
  if (!state.writer) {
    state.writer = createMessageWriter(state.api);
  }
  const messageNumber = state.currentMessageCounter + 1;
  postProgressStatus(state, 'Processing message ' + messageNumber + ' after parsing ' + state.parsedQuadCount + ' quad(s)\u2026', state.processedMessageCount === 0);
  const inference = state.statefulMaterialization
    ? await state.reasoner.inferAsyncWithDiagnostics(state.currentMessage, {
        store: {
          name: state.statefulStoreName,
          clear: state.processedMessageCount === 0,
        },
      })
    : state.reasoner.inferWithDiagnostics(state.currentMessage);
  state.inconsistencyComments.push(formatInconsistencyComments(inference.inconsistencies, 'message ' + messageNumber));
  state.writer.addMessage(inference.quads);
  state.inferredCount += inference.quads.length;
  state.processedMessageCount += 1;
  postProgressStatus(state, 'Processed message ' + messageNumber + '; ' + state.processedMessageCount + ' message(s), ' + state.parsedQuadCount + ' input quad(s), ' + state.inferredCount + ' inferred' + statefulStoreSummary(state) + '\u2026');
}

function statefulStoreSummary(state) {
  return state.statefulMaterialization ? ', stateful store enabled' : '';
}

function countLabel(count, singular) {
  return count + ' ' + singular + (count === 1 ? '' : 's');
}

function postProgressStatus(state, message, force = false) {
  const now = performance.now();
  if (!force && now - state.lastStatusAt < 500) {
    return;
  }
  state.lastStatusAt = now;
  self.postMessage({ type: 'status', message });
}

function formatWorkerDuration(ms) {
  if (ms < 1000) {
    return ms.toFixed(0) + ' ms';
  }
  const seconds = ms / 1000;
  if (seconds < 60) {
    return seconds.toFixed(1) + ' s';
  }
  const minutes = Math.floor(seconds / 60);
  const wholeSeconds = Math.floor(seconds % 60).toString().padStart(2, '0');
  return minutes + ' min ' + wholeSeconds + ' s';
}

function createMessageWriter(api) {
  return new api.Writer({
    write(chunk, _encoding, callback) {
      self.postMessage({ type: 'append', chunk });
      callback?.(null);
    },
    end(callback) {
      callback?.(null, '');
    },
  }, { prefixes: outputPrefixes(), rdfMessages: true });
}

function endWriter(writer) {
  return new Promise((resolve, reject) => {
    writer.end((error) => error ? reject(error) : resolve());
  });
}

function appendInconsistencyComments(comments) {
  const text = comments.filter(Boolean).join('');
  if (text) {
    self.postMessage({ type: 'append', chunk: text });
  }
}

function diagnosticStatusSuffix(comments) {
  const text = Array.isArray(comments) ? comments.join('') : comments;
  const count = (text.match(/^# Inconsistency detected/gm) || []).length;
  return count > 0 ? ', found ' + count + ' inconsistency diagnostic(s)' : '';
}

const RDF_TYPE_IRI = 'http://www.w3.org/1999/02/22-rdf-syntax-ns#type';
const OWL_SAME_AS_IRI = 'http://www.w3.org/2002/07/owl#sameAs';
const OWL_DIFFERENT_FROM_IRI = 'http://www.w3.org/2002/07/owl#differentFrom';
const OWL_ALL_DIFFERENT_IRI = 'http://www.w3.org/2002/07/owl#AllDifferent';
const OWL_ALL_DISJOINT_CLASSES_IRI = 'http://www.w3.org/2002/07/owl#AllDisjointClasses';
const OWL_ALL_DISJOINT_PROPERTIES_IRI = 'http://www.w3.org/2002/07/owl#AllDisjointProperties';
const OWL_IRREFLEXIVE_PROPERTY_IRI = 'http://www.w3.org/2002/07/owl#IrreflexiveProperty';
const OWL_ASYMMETRIC_PROPERTY_IRI = 'http://www.w3.org/2002/07/owl#AsymmetricProperty';
const OWL_PROPERTY_DISJOINT_WITH_IRI = 'http://www.w3.org/2002/07/owl#propertyDisjointWith';
const OWL_SOURCE_INDIVIDUAL_IRI = 'http://www.w3.org/2002/07/owl#sourceIndividual';
const OWL_ASSERTION_PROPERTY_IRI = 'http://www.w3.org/2002/07/owl#assertionProperty';
const OWL_TARGET_INDIVIDUAL_IRI = 'http://www.w3.org/2002/07/owl#targetIndividual';
const OWL_TARGET_VALUE_IRI = 'http://www.w3.org/2002/07/owl#targetValue';
const OWL_NOTHING_IRI = 'http://www.w3.org/2002/07/owl#Nothing';
const OWL_THING_IRI = 'http://www.w3.org/2002/07/owl#Thing';
const OWL_COMPLEMENT_OF_IRI = 'http://www.w3.org/2002/07/owl#complementOf';
const OWL_MAX_CARDINALITY_IRI = 'http://www.w3.org/2002/07/owl#maxCardinality';
const OWL_MAX_QUALIFIED_CARDINALITY_IRI = 'http://www.w3.org/2002/07/owl#maxQualifiedCardinality';
const OWL_ON_PROPERTY_IRI = 'http://www.w3.org/2002/07/owl#onProperty';
const OWL_ON_CLASS_IRI = 'http://www.w3.org/2002/07/owl#onClass';
const OWL_ON_DATA_RANGE_IRI = 'http://www.w3.org/2002/07/owl#onDataRange';
const XSD_STRING_IRI = 'http://www.w3.org/2001/XMLSchema#string';
const XSD_NON_NEGATIVE_INTEGER_IRI = 'http://www.w3.org/2001/XMLSchema#nonNegativeInteger';
const GENERATED_SKOLEM_IRI_PREFIX = 'https://eyereasoner.github.io/.well-known/genid/';

function formatInconsistencyComments(reports, context) {
  if (!reports || reports.length === 0) {
    return '';
  }
  const prefix = context ? ' in ' + context : '';
  const visible = reports.slice(0, 20);
  const lines = [];
  visible.forEach((report, index) => {
    const rule = report.rule ? shortIri(report.rule) : 'unknown rule';
    const description = describeInconsistencyRule(rule);
    lines.push('# Inconsistency detected' + prefix + ': ' + summarizeInconsistency(report, rule, description));
    lines.push('#   Rule: ' + rule + (description ? ' \u2014 ' + description : ''));
    const evidence = externalEvidenceForReport(report, rule);
    if (evidence.length > 0) {
      lines.push('#   Why this fails (public evidence):');
      evidence.forEach((item, itemIndex) => lines.push('#     ' + (itemIndex + 1) + '. ' + item));
    } else {
      const publicTerms = publicDiagnosticTerms(report.terms || []);
      if (publicTerms.length > 0) {
        lines.push('#   Public terms: ' + publicTerms.map(termToComment).join(', '));
      }
      lines.push('#   Why this fails: no public triple-only explanation is available for this rule.');
    }
    if (hasHiddenDiagnosticTerms(report.terms || [])) {
      lines.push('#   Internal OWL helper terms were hidden from this explanation.');
    }
    if (index < visible.length - 1) {
      lines.push('#');
    }
  });
  if (reports.length > visible.length) {
    lines.push('# ... ' + (reports.length - visible.length) + ' more inconsistency diagnostic(s) omitted.');
  }
  return lines.map(commentLine).join('\\n') + '\\n\\n';
}

function summarizeInconsistency(report, rule, fallbackDescription) {
  const terms = report.terms || [];
  if (rule === 'eq-diff1') {
    return safeTermLabel(terms[0]) + ' is asserted as both owl:sameAs and owl:differentFrom ' + safeTermLabel(terms[1]) + '.';
  }
  if (rule === 'eq-diff2' || rule === 'eq-diff3') {
    return safeTermLabel(terms[1]) + ' and ' + safeTermLabel(terms[2]) + ' are listed as different but are also owl:sameAs.';
  }
  if (rule === 'prp-irp') {
    return safeTermLabel(terms[1]) + ' uses irreflexive property ' + safeTermLabel(terms[0]) + ' on itself.';
  }
  if (rule === 'prp-asyp') {
    return safeTermLabel(terms[0]) + ' is asymmetric, but it relates ' + safeTermLabel(terms[1]) + ' to ' + safeTermLabel(terms[2]) + ' in both directions.';
  }
  if (rule === 'prp-pdw') {
    return safeTermLabel(terms[0]) + ' and ' + safeTermLabel(terms[1]) + ' are disjoint properties used for the same subject/object pair.';
  }
  if (rule === 'prp-adp') {
    return safeTermLabel(terms[1]) + ' and ' + safeTermLabel(terms[2]) + ' are all-disjoint properties used for the same subject/object pair.';
  }
  if (rule === 'prp-npa1' || rule === 'prp-npa2') {
    return 'a negative property assertion is contradicted by an actual property value.';
  }
  if (rule === 'cls-nothing2') {
    return safeTermLabel(terms[0]) + ' is typed as owl:Nothing.';
  }
  if (rule === 'cls-com') {
    return safeTermLabel(terms[2]) + ' is typed as both a class and its complement.';
  }
  if (rule === 'cls-maxc1' || rule === 'cls-maxqc1' || rule === 'cls-maxqc2' || rule === 'cls-maxqd1') {
    return safeTermLabel(terms[1]) + ' has a value for ' + safeTermLabel(terms[2]) + ' although a maximum-cardinality 0 restriction applies.';
  }
  if (rule === 'cax-dw') {
    return safeTermLabel(terms[2]) + ' is typed as both disjoint classes ' + safeTermLabel(terms[0]) + ' and ' + safeTermLabel(terms[1]) + '.';
  }
  if (rule === 'cax-adc') {
    return safeTermLabel(terms[3]) + ' is typed as two classes from an owl:AllDisjointClasses axiom.';
  }
  if (rule === 'dt-not-type') {
    return safeTermLabel(terms[0]) + ' is not a valid value for datatype ' + safeTermLabel(terms[1]) + '.';
  }
  return fallbackDescription ? fallbackDescription + '.' : 'an OWL 2 RL contradiction was derived.';
}

function externalEvidenceForReport(report, rule) {
  const terms = report.terms || [];
  const evidence = [];
  if (rule === 'eq-diff1') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_SAME_AS_IRI), terms[1]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_DIFFERENT_FROM_IRI), terms[1]);
  } else if (rule === 'eq-diff2') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_ALL_DIFFERENT_IRI));
    addEvidenceText(evidence, [terms[0], terms[1], terms[2]], 'The owl:members list of ' + termToComment(terms[0]) + ' contains ' + termToComment(terms[1]) + ' and ' + termToComment(terms[2]) + '.');
    addEvidenceTriple(evidence, terms[1], namedTerm(OWL_SAME_AS_IRI), terms[2]);
  } else if (rule === 'eq-diff3') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_ALL_DIFFERENT_IRI));
    addEvidenceText(evidence, [terms[0], terms[1], terms[2]], 'The owl:distinctMembers list of ' + termToComment(terms[0]) + ' contains ' + termToComment(terms[1]) + ' and ' + termToComment(terms[2]) + '.');
    addEvidenceTriple(evidence, terms[1], namedTerm(OWL_SAME_AS_IRI), terms[2]);
  } else if (rule === 'prp-irp') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_IRREFLEXIVE_PROPERTY_IRI));
    addEvidenceTriple(evidence, terms[1], terms[0], terms[1]);
  } else if (rule === 'prp-asyp') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_ASYMMETRIC_PROPERTY_IRI));
    addEvidenceTriple(evidence, terms[1], terms[0], terms[2]);
    addEvidenceTriple(evidence, terms[2], terms[0], terms[1]);
  } else if (rule === 'prp-pdw') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_PROPERTY_DISJOINT_WITH_IRI), terms[1]);
    addEvidenceTriple(evidence, terms[2], terms[0], terms[3]);
    addEvidenceTriple(evidence, terms[2], terms[1], terms[3]);
  } else if (rule === 'prp-adp') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_ALL_DISJOINT_PROPERTIES_IRI));
    addEvidenceText(evidence, [terms[0], terms[1], terms[2]], 'The owl:members list of ' + termToComment(terms[0]) + ' contains properties ' + termToComment(terms[1]) + ' and ' + termToComment(terms[2]) + '.');
    addEvidenceTriple(evidence, terms[3], terms[1], terms[4]);
    addEvidenceTriple(evidence, terms[3], terms[2], terms[4]);
  } else if (rule === 'prp-npa1') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_SOURCE_INDIVIDUAL_IRI), terms[1]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ASSERTION_PROPERTY_IRI), terms[2]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_TARGET_INDIVIDUAL_IRI), terms[3]);
    addEvidenceTriple(evidence, terms[1], terms[2], terms[3]);
  } else if (rule === 'prp-npa2') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_SOURCE_INDIVIDUAL_IRI), terms[1]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ASSERTION_PROPERTY_IRI), terms[2]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_TARGET_VALUE_IRI), terms[3]);
    addEvidenceTriple(evidence, terms[1], terms[2], terms[3]);
  } else if (rule === 'cls-nothing2') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_NOTHING_IRI));
  } else if (rule === 'cls-com') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_COMPLEMENT_OF_IRI), terms[1]);
    addEvidenceTriple(evidence, terms[2], namedTerm(RDF_TYPE_IRI), terms[0]);
    addEvidenceTriple(evidence, terms[2], namedTerm(RDF_TYPE_IRI), terms[1]);
  } else if (rule === 'cls-maxc1') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_MAX_CARDINALITY_IRI), cardinalityZeroTerm());
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_PROPERTY_IRI), terms[2]);
    addEvidenceTriple(evidence, terms[1], namedTerm(RDF_TYPE_IRI), terms[0]);
    addEvidenceTriple(evidence, terms[1], terms[2], terms[3]);
  } else if (rule === 'cls-maxqc1') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_MAX_QUALIFIED_CARDINALITY_IRI), cardinalityZeroTerm());
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_PROPERTY_IRI), terms[2]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_CLASS_IRI), terms[4]);
    addEvidenceTriple(evidence, terms[1], namedTerm(RDF_TYPE_IRI), terms[0]);
    addEvidenceTriple(evidence, terms[1], terms[2], terms[3]);
    addEvidenceTriple(evidence, terms[3], namedTerm(RDF_TYPE_IRI), terms[4]);
  } else if (rule === 'cls-maxqc2') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_MAX_QUALIFIED_CARDINALITY_IRI), cardinalityZeroTerm());
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_PROPERTY_IRI), terms[2]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_CLASS_IRI), namedTerm(OWL_THING_IRI));
    addEvidenceTriple(evidence, terms[1], namedTerm(RDF_TYPE_IRI), terms[0]);
    addEvidenceTriple(evidence, terms[1], terms[2], terms[3]);
  } else if (rule === 'cls-maxqd1') {
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_MAX_QUALIFIED_CARDINALITY_IRI), cardinalityZeroTerm());
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_PROPERTY_IRI), terms[2]);
    addEvidenceTriple(evidence, terms[0], namedTerm(OWL_ON_DATA_RANGE_IRI), terms[4]);
    addEvidenceTriple(evidence, terms[1], namedTerm(RDF_TYPE_IRI), terms[0]);
    addEvidenceTriple(evidence, terms[1], terms[2], terms[3]);
    addEvidenceTriple(evidence, terms[3], namedTerm(RDF_TYPE_IRI), terms[4]);
  } else if (rule === 'cax-dw') {
    addEvidenceTriple(evidence, terms[0], namedTerm('http://www.w3.org/2002/07/owl#disjointWith'), terms[1]);
    addEvidenceTriple(evidence, terms[2], namedTerm(RDF_TYPE_IRI), terms[0]);
    addEvidenceTriple(evidence, terms[2], namedTerm(RDF_TYPE_IRI), terms[1]);
  } else if (rule === 'cax-adc') {
    addEvidenceTriple(evidence, terms[0], namedTerm(RDF_TYPE_IRI), namedTerm(OWL_ALL_DISJOINT_CLASSES_IRI));
    addEvidenceText(evidence, [terms[0], terms[1], terms[2]], 'The owl:members list of ' + termToComment(terms[0]) + ' contains classes ' + termToComment(terms[1]) + ' and ' + termToComment(terms[2]) + '.');
    addEvidenceTriple(evidence, terms[3], namedTerm(RDF_TYPE_IRI), terms[1]);
    addEvidenceTriple(evidence, terms[3], namedTerm(RDF_TYPE_IRI), terms[2]);
  } else if (rule === 'dt-not-type') {
    addEvidenceText(evidence, [terms[0], terms[1]], termToComment(terms[0]) + ' is explicitly typed as ' + termToComment(terms[1]) + ', but the lexical value is not valid for that datatype.');
  }
  return evidence;
}

function addEvidenceTriple(target, subject, predicate, object) {
  if (!subject || !predicate || !object || subject.termType === 'Literal' || hasHiddenDiagnosticTerms([subject, predicate, object])) {
    return;
  }
  target.push(termToComment(subject) + ' ' + termToComment(predicate) + ' ' + termToComment(object) + ' .');
}

function addEvidenceText(target, terms, text) {
  if (hasHiddenDiagnosticTerms(terms)) {
    return;
  }
  target.push(text);
}

function publicDiagnosticTerms(terms) {
  return terms.filter((term) => term && !isHiddenDiagnosticTerm(term));
}

function hasHiddenDiagnosticTerms(terms) {
  return terms.some(isHiddenDiagnosticTerm);
}

function isHiddenDiagnosticTerm(term) {
  return Boolean(term && term.termType === 'NamedNode' && term.value.indexOf(GENERATED_SKOLEM_IRI_PREFIX) === 0);
}

function safeTermLabel(term) {
  return term && !isHiddenDiagnosticTerm(term) ? termToComment(term) : 'a hidden OWL helper node';
}

function namedTerm(value) {
  return { termType: 'NamedNode', value };
}

function cardinalityZeroTerm() {
  return { termType: 'Literal', value: '0', language: '', datatype: namedTerm(XSD_NON_NEGATIVE_INTEGER_IRI) };
}

function commentLine(value) {
  return String(value).replace(/[\\r\\n]+/g, ' ');
}

function shortIri(value) {
  const match = String(value).match(/[/#]([^/#]+)$/);
  return match ? match[1] : String(value);
}

function describeInconsistencyRule(rule) {
  return {
    'eq-diff1': 'same individual is also differentFrom',
    'eq-diff2': 'AllDifferent members are sameAs',
    'eq-diff3': 'AllDifferent distinctMembers are sameAs',
    'prp-irp': 'irreflexive property used reflexively',
    'prp-asyp': 'asymmetric property used in both directions',
    'prp-pdw': 'disjoint properties share a subject/object pair',
    'prp-adp': 'AllDisjointProperties members share a subject/object pair',
    'prp-npa1': 'negative object property assertion is contradicted',
    'prp-npa2': 'negative data property assertion is contradicted',
    'cls-nothing2': 'resource is typed as owl:Nothing',
    'cls-com': 'complement classes share an instance',
    'cls-maxc1': 'maxCardinality 0 restriction has a value',
    'cls-maxqc1': 'qualified maxCardinality 0 restriction has a value of the qualified class',
    'cls-maxqc2': 'qualified maxCardinality 0 restriction has a value',
    'cls-maxqd1': 'qualified maxCardinality 0 data restriction has a typed value',
    'cax-dw': 'disjoint classes share an instance',
    'cax-adc': 'AllDisjointClasses members share an instance',
    'dt-not-type': 'literal is not valid for its datatype',
  }[rule] || '';
}

function termToComment(term) {
  if (!term) {
    return 'unknown';
  }
  if (term.termType === 'NamedNode') {
    return prefixedName(term.value) || '<' + term.value + '>';
  }
  if (term.termType === 'BlankNode') {
    return '_:' + term.value;
  }
  if (term.termType === 'Literal') {
    let literal = JSON.stringify(term.value);
    if (term.language) {
      literal += '@' + term.language;
    } else if (term.datatype && term.datatype.value !== XSD_STRING_IRI) {
      literal += '^^' + termToComment(term.datatype);
    }
    return literal;
  }
  return term.termType + ':' + term.value;
}

function prefixedName(value) {
  const prefixes = Object.assign({
    rdf: 'http://www.w3.org/1999/02/22-rdf-syntax-ns#',
    xsd: 'http://www.w3.org/2001/XMLSchema#',
  }, outputPrefixes());
  const names = Object.keys(prefixes).sort((left, right) => prefixes[right].length - prefixes[left].length);
  for (const name of names) {
    const iri = prefixes[name];
    if (value.indexOf(iri) === 0) {
      const local = value.slice(iri.length);
      if (/^[A-Za-z_][A-Za-z0-9._-]*$/.test(local)) {
        return name + ':' + local;
      }
    }
  }
  return '';
}

function progressMessage(state, prefix) {
  if (state.messagesMode) {
    return prefix + '; parsed ' + state.parsedQuadCount + ' RDF Messages quad(s), processed ' + state.processedMessageCount + ' message(s), emitted ' + state.inferredCount + ' inferred quad(s)\u2026';
  }
  return prefix + '; parsed ' + state.parsedQuadCount + ' input quad(s)\u2026';
}

function outputPrefixes() {
  return {
    transit: 'https://example.org/transit#',
    logistics: 'https://example.org/logistics#',
    subjects: 'https://example.org/subjects#',
    catalog: 'https://example.org/catalog#',
    family: 'https://example.org/family#',
    test: 'https://example.org/test#',
    shacl: 'https://example.org/shacl#',
    gen: 'https://eyereasoner.github.io/.well-known/genid/',
    skos: 'http://www.w3.org/2004/02/skos/core#',
    sh: 'http://www.w3.org/ns/shacl#',
    rdfs: 'http://www.w3.org/2000/01/rdf-schema#',
    owl: 'http://www.w3.org/2002/07/owl#',
    xsd: 'http://www.w3.org/2001/XMLSchema#',
  };
}
`,n=new Blob([e],{type:"text/javascript"});return new Worker(URL.createObjectURL(n))}async function ie(e,n){if(d(e)==="text")return e==="background"?r.backgroundText.getValue():r.dataText.getValue();let o=(e==="background"?t.backgroundUrl:t.dataUrl).value.trim();if(!o)throw new Error(`Enter a ${e==="background"?"background RDF":"data"} URL or switch to text input.`);g(`Fetching ${e==="background"?"background RDF":"input data"} before processing\u2026`);let i=await fetch(o,{signal:n});if(!i.ok)throw new Error(`Could not fetch ${o}: ${i.status} ${i.statusText}`);return i.text()}function pe(){w&&(g("Stopping after the current worker step\u2026"),t.stopButton.disabled=!0,w.controller.abort())}function F(e){t.runButton.disabled=e,t.stopButton.hidden=!e,t.stopButton.disabled=!e}function Y(e,n){e.statusMessage=n,K(e)}function de(e){I(e),e.elapsedTimer=window.setInterval(()=>I(e),250)}function D(e){e.finishedAt===void 0&&(e.finishedAt=performance.now()),e.elapsedTimer!==void 0&&(window.clearInterval(e.elapsedTimer),e.elapsedTimer=void 0),I(e)}function I(e){t.runtimeStats.textContent="",t.runtimeStats.hidden=!0,K(e)}function K(e){if(!e.statusMessage)return;let n=e.finishedAt===void 0?"Elapsed":"Total elapsed",s=[H(e.statusMessage),`${n} ${he(ue(e))}`];e.runtimeMessage&&s.push(H(e.runtimeMessage)),g(s.filter(Boolean).join(" \xB7 "))}function H(e){return e.trim().replace(/[.。]\s*$/,"")}function ce(e){b+=e,m||(m=window.setTimeout(q,100))}function q(){if(m&&(window.clearTimeout(m),m=0),!b)return;let e=b;if(b="",r.outputText.replaceRange&&r.outputText.lastLine&&r.outputText.getLine){let n=r.outputText.lastLine();r.outputText.replaceRange(e,{line:n,ch:r.outputText.getLine(n).length})}else r.outputText.setValue(r.outputText.getValue()+e)}function _(){m&&(window.clearTimeout(m),m=0),b=""}function ue(e){return(e.finishedAt??performance.now())-e.startedAt}function he(e){let n=Math.max(0,e/1e3);if(n<10)return`${n.toFixed(1)} s`;if(n<60)return`${n.toFixed(0)} s`;let s=Math.floor(n/60),a=Math.floor(n%60).toString().padStart(2,"0");return`${s} min ${a} s`}function me(e){if(e.aborted)throw new DOMException("Inference was stopped.","AbortError")}function fe(e){return e instanceof DOMException&&e.name==="AbortError"}function O(){T(t.backgroundUrlPanel,d("background")==="url"),T(t.backgroundTextPanel,d("background")==="text"),T(t.dataUrlPanel,d("data")==="url"),T(t.dataTextPanel,d("data")==="text"),window.setTimeout(()=>{r.backgroundText.refresh(),r.dataText.refresh(),r.shaclInText.refresh(),r.shaclOutText.refresh(),r.outputText.refresh()},0)}function T(e,n){e.hidden=!n}function d(e){return(e==="background"?t.backgroundMode.value:t.dataMode.value)==="url"?"url":"text"}function ge(){let e=f();h=!0,t.exampleSelect.value=e.id,t.backgroundMode.value=u.backgroundMode,t.dataMode.value=u.dataMode,t.statefulMaterialization.checked=!1,t.backgroundUrl.value="",t.dataUrl.value="",V([]),r.backgroundText.setValue(e.background),r.dataText.setValue(e.data),r.shaclInText.setValue(e.shaclIn??""),r.shaclOutText.setValue(e.shaclOut??""),t.shapeHintsPanel.open=!!(e.shaclIn||e.shaclOut),r.outputText.setValue(""),h=!1,O(),P(),g(`Reset to ${e.label}.`)}function we(){t.exampleSelect.textContent="";for(let e of S){let n=document.createElement("option");n.value=e.id,n.textContent=`${e.label} \u2014 ${$(e)}`,t.exampleSelect.appendChild(n)}t.exampleSelect.value=f().id}function $(e){let n=[e.backgroundFile,e.dataFile];return e.shaclInFile&&n.push(e.shaclInFile),e.shaclOutFile&&n.push(e.shaclOutFile),n.join(" + ")}function ye(){if(!t.ruleProfileList){R();return}t.ruleProfileList.textContent="";for(let e of C){let n=xe(e.file),s=`ruleProfile-${X(e.file)}`,a=document.createElement("div");a.className="rule-profile-option";let o=document.createElement("input");o.type="checkbox",o.id=s,o.checked=!0,o.dataset.ruleFile=e.file,o.addEventListener("change",()=>{R(),y()});let i=document.createElement("div");i.className="rule-profile-copy";let p=document.createElement("label");p.htmlFor=s,p.textContent=n.label;let l=document.createElement("span");l.className="hint",l.textContent=n.description,i.append(p,l),a.append(o,i),t.ruleProfileList.appendChild(a)}R()}function xe(e){return{"owl2rl-eyeling.n3":{label:"OWL 2 RL / RDF rules",description:"Materializes OWL 2 RL and RDFS-style consequences such as subclass, subproperty, domain/range, equivalence, property chains, selected restrictions, sameAs, and inconsistency diagnostics."},"skos-entailment.n3":{label:"SKOS Core entailment rules",description:"Materializes SKOS Core consequences such as concept-scheme membership, broader/narrower inverses and transitive closures, semantic relation hierarchy, labels, notes, collections, and mapping properties."},"shacl-core-eyeling.n3":{label:"SHACL Core validation rules",description:"Runs the Eyeling-targeted SHACL Core validation profile and emits sh:ValidationResult triples. It passes the W3C SHACL Core suite by the harness sh:conforms criterion."},"shacl12-core-eyeling.n3":{label:"SHACL 1.2 Core draft additions",description:"Adds draft SHACL 1.2 Core extensions on top of the SHACL Core profile, including ShapeClass targets, explicit sh:shape targets, constant sh:values/defaultValue, and sh:singleLine."}}[e]??{label:e,description:"Additional bundled N3 rule profile loaded from the repository rules folder."}}function Q(){return t.ruleProfileList?Array.from(t.ruleProfileList.querySelectorAll('input[type="checkbox"][data-rule-file]')):[]}function Z(){let e=new Set(J());return C.filter(n=>!e.has(n.file))}function J(){return Q().filter(e=>!e.checked).map(e=>e.dataset.ruleFile??"").filter(Boolean).sort()}function V(e){let n=new Set(e);for(let s of Q())s.checked=!n.has(s.dataset.ruleFile??"");R()}function R(){if(!t.rulesSummary)return;let e=Z().map(n=>n.file);e.length===E.length?t.rulesSummary.textContent=`Rule profiles: all ${e.length} enabled`:e.length===0?t.rulesSummary.textContent="Rule profiles: none enabled":t.rulesSummary.textContent=`Rule profiles: ${e.length}/${E.length} enabled`}function be(e){let n=v(e)??f();h=!0,N(n),h=!1,O(),P(),g(`Loaded ${n.label} from ${$(n)}.`)}function N(e){t.exampleSelect.value=e.id,t.backgroundMode.value="text",t.dataMode.value="text",t.statefulMaterialization.checked=ee(e.id),t.backgroundUrl.value="",t.dataUrl.value="",r.backgroundText.setValue(e.background),r.dataText.setValue(e.data),r.shaclInText.setValue(e.shaclIn??""),r.shaclOutText.setValue(e.shaclOut??""),t.shapeHintsPanel.open=!!(e.shaclIn||e.shaclOut),r.outputText.setValue("")}function ee(e){return e==="stateful-materialization"}function f(){return v("owl-skos-catalog")??S[0]}function v(e){return S.find(n=>n.id===e)}function ve(){let e=v(t.exampleSelect.value),n=J();if(e&&U(e)&&n.length===0)return e.id===f().id?{}:{example:e.id};let s={disabledRuleFiles:n.length>0?n:void 0,backgroundMode:d("background")===u.backgroundMode?void 0:d("background"),dataMode:d("data")===u.dataMode?void 0:d("data"),statefulMaterialization:t.statefulMaterialization.checked||void 0,backgroundUrl:t.backgroundUrl.value.trim()||void 0,dataUrl:t.dataUrl.value.trim()||void 0},a=r.backgroundText.getValue(),o=r.dataText.getValue(),i=r.shaclInText.getValue(),p=r.shaclOutText.getValue();return e&&U(e)&&e.id!==f().id&&(s.example=e.id),!s.example&&a!==u.backgroundText&&(s.backgroundText=a),!s.example&&o!==u.dataText&&(s.dataText=o),i.trim()&&(s.shaclInText=i),p.trim()&&(s.shaclOutText=p),s}function U(e){return d("background")==="text"&&d("data")==="text"&&t.statefulMaterialization.checked===ee(e.id)&&t.backgroundUrl.value.trim()===""&&t.dataUrl.value.trim()===""&&r.backgroundText.getValue()===e.background&&r.dataText.getValue()===e.data&&r.shaclInText.getValue()===(e.shaclIn??"")&&r.shaclOutText.getValue()===(e.shaclOut??"")}function Ce(){let e=Te(window.location.hash);if(e){let s=v(e);if(s){h=!0,N(s),V([]),h=!1;return}}let n=ke(window.location.hash);if(n){if(h=!0,n.example){let s=v(n.example);s&&N(s)}t.backgroundMode.value=n.backgroundMode??u.backgroundMode,t.dataMode.value=n.dataMode??u.dataMode,n.statefulMaterialization!==void 0&&(t.statefulMaterialization.checked=n.statefulMaterialization),t.backgroundUrl.value=n.backgroundUrl??"",t.dataUrl.value=n.dataUrl??"",n.backgroundText!==void 0&&r.backgroundText.setValue(n.backgroundText),n.dataText!==void 0&&r.dataText.setValue(n.dataText),n.shaclInText!==void 0&&(r.shaclInText.setValue(n.shaclInText),t.shapeHintsPanel.open=!0),n.shaclOutText!==void 0&&(r.shaclOutText.setValue(n.shaclOutText),t.shapeHintsPanel.open=!0),V(n.disabledRuleFiles??[]),h=!1}}function y(){h||(window.clearTimeout(z),z=window.setTimeout(P,400))}function P(){let e=Se(ve()),n=`${window.location.pathname}${window.location.search}${e?`#${e}`:""}`;window.history.replaceState(null,"",n)}function Se(e){if(e.example&&Object.keys(e).length===1)return`example=${encodeURIComponent(e.example)}`;let n=JSON.stringify(e);return n==="{}"?"":`state=${btoa(unescape(encodeURIComponent(n))).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"")}`}function Te(e){let n=ne(e,"#example=");if(!n)return"";try{return decodeURIComponent(n)}catch{return""}}function ke(e){let n=ne(e,"#state=");if(!n)return null;try{let s=n.replace(/-/g,"+").replace(/_/g,"/").padEnd(Math.ceil(n.length/4)*4,"=");return JSON.parse(decodeURIComponent(escape(atob(s))))}catch{return null}}function ne(e,n){if(!e.startsWith(n))return"";let s=e.slice(n.length),a=s.indexOf("?");return a===-1?s:s.slice(0,a)}function g(e){t.status.textContent=e}function M(e){return c(e)}function A(e){return c(e)}function k(e){return c(e)}function c(e){let n=document.getElementById(e);if(!n)throw new Error(`Missing element #${e}`);return n}function G(e){return document.getElementById(e)}});Re();})();
